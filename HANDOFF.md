# HANDOFF

作業状況・次の手順・保留事項。区切りごとに更新して push する。

最終更新: 2026-09-21（レポートを標準層・独自層の 2 本に分割）

---

## 現在地

**Track A（ELM327）は完成。** 標準 OBD 層は実測で確定、ギヤ判定は実走 5 本
13,000 行超で検証済み（判定窓を段ごとに分けて確定 98.1%）。

**Track B（自作基板）は目的を達成した。** 2026-09-17 に実車で独自層を掴み、
**09-19 の実走 2 本（13 分 / 3837 サンプル）で 4 つの目的すべてが決着した** ——
index 4 の追従、**比推定の正解ラベルの取得**、index 9 が段数でないこと、
車速バイトが標準層の `0D` と同じ信号であること。実測は
[`docs/FINDINGS.md`](docs/FINDINGS.md)、判定への波及は
[`docs/gear.md`](docs/gear.md)、実装仕様は [`firmware/SPEC.md`](firmware/SPEC.md)。

### 実測で確定したこと

詳細は [FINDINGS.md](docs/FINDINGS.md)。要点のみ。

- プロトコルは **ISO 14230-4 (KWP FAST)**、ECU アドレス `0x10`
- 対応 PID は **24 個**。`0x01`〜`0xFF` の総当たり 264 リクエストで未申告 PID はゼロ
- **サービス22 は全 DID `NO DATA`**。`0902`(VIN) 非対応、DTC なし
- CalID `0102A20601` / CVN `AA42A9BE` / ECU 名 `ECM -EngineContr…`
- **`0D`(車速) と `11`(スロットル絶対) はエンジン停止だと `NO DATA`**。始動後は正常
- サンプリング上限は **約 6.3Hz（158ms/req）**。マルチ PID 一括要求は非対応
- **`0B`(吸気管圧力) は使えない。** 単気筒の吸気脈動を平滑せず瞬時値で返すため、この
  サンプリング周期ではエイリアシングする（アイドル定常で 39〜92kPa に振れる）
- `45`(相対スロットル) は常時 0.0。実装されていない疑い
- 燃料補正 `06`/`07` は動く（STFT -10.2%、LTFT -14.1% を観測）

### 作ったもの

| | 状態 |
|---|---|
| `tools/pidscan.py` | 対応 PID 全数探索。実機検証済み |
| `tools/logger.py` | 連続ポーリング CSV 出力。実機検証済み |
| `tools/term.py` | 対話 AT ターミナル。実機検証済み |
| `tools/gearcal.py` | ギヤ較正（単一係数 k のフィット）。**k を定数化したので役割は終えた** |
| `tools/p3sweep.py` | 要求前の待ちを掃引。P3min 説の棄却に使った |
| `tools/gearing.py` | 走行余力の表と図を生成。`docs/GEARING.md` の全数値を再現。実機不要 |
| `tools/importlogs.py` | ダウンロード先の走行ログを `private/logs/` へ集める。一覧も出す |
| `tools/cutlog.py` | 走行ログの時間区間を切り出す（CSV と GPX を同時に） |
| `tools/shifttime.py` | 絶対時刻をずらす。公開用に走行日時を伏せる |
| `index.html` | スマホ用モニタ `v20260908b`。**実走 3 時間で動作確認済み** |
| `map.html` | 走行ログビュワー `v20260908b`。トンネルの測位処理と経路補完まで入った |
| `results/` | 停車時のスキャン 3 本、ログ 3 本 |
| `docs/` | 資料の置き場。`FINDINGS.md` `GEARING.md` `gear.md` `monitor.md` `viewer.md` ＋ 図 5 枚（`gearing.py --svg` で再生成）＋ スクリーンショット 2 枚 |

**Track B（基板）側**

| | 状態 |
|---|---|
| `firmware/` | `explore`（探索＋記録）と `selftest`（ベンチ）。実機で通し確認済み |
| `firmware/SPEC.md` | **実装仕様書。** 何が実装されているかをコードから起こしたもの（図は mermaid） |
| `tools/klconsole.py` | BLE 対話コンソール。`!` 以外は素通しなのでコマンドを増やしても直さなくていい |
| `tools/klwatch.py` | `poll` の実時間監視。**動いたバイトを探す計器**（種類 1=定数 2=旗 多い=量） |
| `tools/kllog.py` | ログの吸い出しと CRC32 検算。既定の出力先は `private/logs` |
| `tools/kldecode.py` | レコード分解・時刻復元・チェックサム検算・生ダンプ CSV |
| `tools/klcsv.py` | **基板のログをビュワーが食える CSV にする。** パートを名前順に連結、`0x11` の 1 フレーム = 1 行、駆動状態を添える、測位 CSV を絶対時刻で突合 |
| `tools/klota.py` | BLE で `ota on` → IP を拾って焼く |
| `rec.html` | **走行の記録を仕込むスマホページ**（Android Chrome / Web Bluetooth）。壁時計を入れる・記録を開始する・測位を端末の時計で残す、の 3 つだけ。**Pixel から接続まで確認済み**（2026-09-17）。記録開始・測位・書き出しは未確認 |
| `tools/bletest.py` | BLE の疎通確認 |
| `tools/mkjlc.py` | EasyEDA の書き出しを JLCPCB の BOM / 実装座標に変換 |

### 実走で確認できたこと（2026-08-28、2 走行）

ログ 2 本（21.6 分 1308 行 / 10.2 分 631 行）。生データは `private/logs/`（git 追跡外）。

- **`0D` は動く。** 最大 82 / 86 km/h。最重要の未検証項目がクリア
- **ブラウザからの BLE 接続とプロトコル確立、記録の書き出しまで一通り動作した**
- **ギヤ判定が成立した。** `k` = 1.0148 / 1.0116（2 走行で 0.32% 差）。6 速すべてに
  クラスタが立ち、予測位置からの偏差は最大 1.5%、ギヤ順の片流れ無し
- **予測が両方当たった。** `k` ≒ 1.0（実測 1.013、差の 1.3% は ECU の想定転がり円周が
  幾何円周より小さいため）、`0D`/GPS ≒ 0.933（実測 0.935）。詳細は FINDINGS.md
- 判定窓は ±4% を既定にした。広げても不確定は 2 割弱までしか下がらず、5-6 速の境界が
  ±6.0% で頭打ちのため。残りはクラッチ・シフト・158ms スキューによる真の過渡

### 2026-08-29 に入れたもの

走行画面 `v20260829d` / ビュワー `v20260829d`。

- **ギヤ判定の取得時刻ズレを修正。** 行を `0x0C` の応答時に書いていたため車速が 1 周期
  （約 520ms）古く、加速中は比が過大・減速中は過小になっていた。エンブレ中の判定失敗の
  主因。`0x0D` の直後に書くよう変え、残差を `skew_ms` 列に記録する
- **ビュワーも同じズレを補正。** 回転数を車速の取得時刻へ線形補間する。新旧フォーマットで
  ズレの符号が逆になる点に対応。ギヤ確定率 56% → 65%
- **表示車速のスプロケット補正。** 前 12-15T の選択（後は 36T 固定）。GPS から丁数を推定
  できる。**補正は表示だけでギヤ判定は生値**
- **判定窓の上限を諸元から算出**（5.81%。隣接段の窓が重ならない限界）
- ビュワーの見た目: ギヤ 6 色、停車はハッチングの帯（地図には描かない）、判定不能は細い
  破線で繋ぐ（停車をまたぐ場合は繋がない）、縦スクロールとの両立
- ファイル名をローカル時刻に（UTC だと朝の走行が前日深夜に見える）

### 机上検討で分かったこと（2026-08-29、走行分布とギヤ判定の精度）

作業物は `work/gearmap*.py` `work/gearseq.py` `work/gearfilter*.py` と `work/gearmap.html` /
`work/gearmap.png`（リポジトリ外。位置情報は含まないが実走由来なので `work/` 止まり）。

**走行の分布**

- 回転数は**どのギヤでも 3,400〜5,500rpm** に収まる。最大トルク 8,000 / 最大出力 9,000 に対し
  **上半分をほぼ使っていない**。ギヤを変えて回転を一定域に保つ走り方
- **車速に対してギヤは一意でない。** 50-60km/h で 3速34% / 4速40% / 6速22%。構成比で見るしかない
- 5速の使用が薄い（62-77km/h の狭い範囲）。4速と6速に挟まれて出番が少ない

**遷移の統計（誤判定を弾く根拠になる）**

- **クラッチが繋がっている間（空白なし）は 99.9% が ±1段以内**（n=851、違反 1 件）
- 飛びは**空白を挟むときにしか起きない**（n=169 中 8.4%）。しかも方向で意味が違い、
  **下方向（-2/-3段）は減速時の連続シフトダウンで妥当、上方向（+2〜+4段）は誤判定**
- ただし**遷移制約だけでは効果が薄い**。52 点を捨てて疑わしい点が 14→8 にしかならない

**回転数と車速の時刻合わせ（重要）**

- **「1 行前の回転数 ＋ 当該行の車速」は場当たりではなく構造的に正しい。** 旧フォーマットは
  行を `0x0C` 応答時に書くため、行 k の車速は行 k-1 の回転数の約 130ms 後に取得されている。
  つまり同一周期の組をそのまま復元している
- **線形補間は効かない。** 狙う時刻を 3 通り（当該行から遡る / 前の行から進める / オフセット掃引）
  で試したが、どれも粗い寄せ（29.8% / 20.3%）に届かなかった。回転変化が穏やかな区間でも同じ
- 理由は**残差 130ms の両側にサンプルが無いこと**。両端が 130ms 前と 390ms 後と非対称で、
  補間は測定値を推定値に置き換える操作になり、線形性の誤差が利得を上回る
- **効かせるには `0C → 0D → 0C` と読んで内挿にする必要がある**（1 周期あたり 1 リクエスト増）。
  次の走行で P3min の結果を見てから、脱落が減るだけで足りるか判断する

### スプロケット丁数の検討（2026-08-29）

**[GEARING.md](docs/GEARING.md) に分離した。** 14T → 15T が走行余力に与える影響を数値化したもの。
計算は `tools/gearing.py`（諸元・推定トルク曲線・仮定値を冒頭に定数として分離）。

- **6 速 1 枚で走れる範囲が 60〜120 km/h → 64〜110 km/h に縮む**（幅で 23% 減）。下端は
  回転数、上端は出力と、両端を決める要因が違うため両側から詰まる
- **万能ギアの役割が 6 速から 5 速へ移り、6 速が巡航専用のオーバードライブになった。**
  15T の 5 速は 14T の 6 速より 4.8% ショートで仕事をそのまま引き受ける。到達性能は不変
- **最高速はほぼ不変**（133 → 132 km/h）。出力律速のため
- ギアリング部分は実走ログの 6 速定常点と中央値 +0.6% で一致。**ただし実車速は最大 92 km/h
  までしかなく、100 km/h 以上は計算のみ**
- **PID 04（Load）は走行余力の指標にならない**（55→95 km/h の定常で 62〜77% と平坦）。
  余力を見るならスロットル開度（PID 11）

諸元は**年式で違う**。2018 年式は **2BK-MC52**（20 kW/9,000rpm、23 N·m/8,000rpm、144 kg）で、
2022-07 以降の 8BK-MC52 は発生回転数が異なる（9,500 / 7,750 rpm）。公式サイトの諸元ページは
現行 8BK なので、そのまま使うと 2018 年式には合わない。

### 2026-08-30 の実測で片付いたこと

**P3min 説は棄却。** `tools/p3sweep.py` を停車＋アイドリングで 3 回流した。詳細は
[FINDINGS.md](docs/FINDINGS.md) の「取得周期と欠測」。

- **待ち時間と欠測率に安定した関係が無い。** 同じ待ち 80ms が 0.0% と 21.5% を行き来する
- **周期は待ちぶんだけ素直に伸びる**ので、欠測を減らせても記録レートは上がらない。
  「脱落を潰せば実効サンプリングが改善する」という見立ては成り立たない
- 記録レートで選ぶと**待ち 0ms が 3 回とも最良**。アプリの既定を 0 に変更済み
- プロトコル `AUTO` の影響は平均で悪い傾向（16.4% 対 9.9%）はあるが、**再現ばらつき
  （最大 21.5pt）と同程度なので確定できない**

**クランキングは BLE リンクごと落とす。** 始動前に接続しておく手順は無意味で、始動して
から接続するしかない。`0100` 捕獲がずっと失敗していた理由もこれ。

**`0100` を IG ON・エンジン停止で取得。** `results/log_20260830_0933_igon_cold.csv`。
水温 22℃、回転数 0、全 138 サンプルが `0100`。~~ただし冷間かつ運転中の `0100` は未取得~~
→ **2026-09-05 に取得。下記参照。**

### 2026-08-30 の走行で決着したこと（3 走行 13,153 行 / 2.9 時間）

判定項目は 3 つとも答えが出た。

| | 旧 2 本 | 新 3 本 |
|---|---:|---:|
| 行数 | 1,939 | **13,153** |
| ギヤ確定（走行中） | 63.2% | **93.7%** |
| 欠測率 | 約 38% | **1.9%** |
| 実効レート | 1.01 Hz | **1.28 Hz** |
| `skew_ms` | 無し | 中央 **182ms**（5–95% 163–255） |

1. **実走 40% 脱落は監視タイマが原因だった。** 走行でも再現せず、1.9% に収まった
2. **時刻ズレ修正は効いた。** `skew` が設計値どおり狭く、判定率が 30pt 上がった
3. **記録レートは 1.28 Hz。** 待ち 0ms の効果も出ている
4. `proto` は全走行で `ISO 14230-4 (KWP FAST)`。`AUTO` へ落ちてはいなかった

### 2026-08-31 に入れたもの

**`0D` は切り捨てだと同定し、`k` を入れ替えた。** 段ごとに `k` を独立に当てると生値では
2速 1.0213〜6速 1.0129 と単調にずれる。剛体連結なら全段同一のはずで、車速へ +0.5 を戻すと
ばらつきが 0.83% → 0.13% に潰れる。**`k` = 1.0148 → 1.0057**、ECU 想定円周 1.894 →
1.911m。旧 `k` は量子化の偏りを吸収した値で、物理的解釈を載せてはいけないものだった。

**`k` を定数化して較正 UI を外した。** 較正すべき自由度が存在しないため。ただし `fitK()` は
診断として残し、ログから当てた値を設定（⚙）に併記する（前提が崩れたときに気づく手段は要る）。

**アイドル回転での誤判定を弾いた。** クラッチを切った惰性走行で回転数がアイドルに張り付き、
比が偶然どれかの段と一致する。回転数 1,700rpm 未満を除外（実測で落ちるのは 0.66%、
その 100% がスロットル全閉）。

**ギヤ判定の許容に車速の量子化ぶんを足した。** `0D` は 1km/h 刻みで ±0.5km/h が比に
`0.5/車速` で効く。既定 4% で判定 92.0% → 94.9%、曖昧点 0.00% のまま。

**トンネルの測位を処理した。** スマホの Geolocation はトンネル内でも測位を返すが精度半径が
数百 m に膨らむ。精度 50m 超と「GPS の移動量が OBD 車速と 25km/h 以上ずれる」の 2 条件で
捨て、そこで線を切る。さらに **OSRM の経路と車速積分**で埋める。経路が使えないときは
直線距離が車速積分と一致すれば直線を引く（トンネルは掘り抜きなのでほぼ直線）。
実測 7 区間すべてを補完。

**チャートが 1 枚も描画されない不具合を直した。** 丁数の自動推定が `buildCharts` より前に
`refresh` を呼び、キャンバス未生成で例外になっていた。`mc52_sf` 未保存のとき＝**その端末で
初めて開いたときだけ**発火し、しかも失敗しても設定は保存されるので**リロードすると直り
再現しない**。時間軸の目盛りとカーソル時刻の表示も追加。

**GEARING.md を更新した。** 突合を 110km/h まで拡張。一方で「実効スロットルと必要トルクの
比が一致するので抗力モデルは合っている」という記述は**撤回**（2 点だけで比を取っていた。
範囲を広げると Nm/開度 が 2.2 倍動く。原因はスロットルの非線形性）。実使用の分布図を追加。

**README にスクリーンショットとリンクを入れた。** 冒頭にアプリ 2 本のリンク、ロガー節と
ビュワー節に実機の画面。ナビ側の目的地・ETA はモザイクで伏せてある。

**公開候補の切り出しを作った。** `tools/cutlog.py` で区間を、`tools/shifttime.py` で日時を
ずらす。`samples/` と `drafts/` は ignore。**公開するのはスクリーンショットだけ**の方針。

### まだ手を付けていないもの

4. ~~**独自層を実車で当てること**~~ → **完了**（停車 09-17 / 実走 09-19）。
   残るのは**噴射時間の絶対単位**（1 タンク分の給油量突合）、`0x20` と `0xD0` の中身
   （別走行）、index 8 bit 2（**index 9 はラジエータファンと同定済み**）
5. サービス `21`/`1A`/`23`/`30`
6. 冷間かつ運転中の `0100`。始動後に接続し、水温が 42℃ に達する前に記録を始める必要がある

### 既知の不具合（次にログを取るときに直す）

7. ~~**`tools/logger.py` が暴走する。**~~ **直した（2026-09-16）。**
   全 PID が `NO DATA` を返すとポーリングループが空転し、有効 161 行に対して
   1250 万行・1.17GB を書いていた（2026-08-30）。**周期を決めていたのが
   ELM327 の応答時間だけ**で、即答で失敗すると待ち時間が消えていた。

   | | |
   |---|---|
   | 周期の床 50ms | どんな理由で速くなっても割らない保険 |
   | **全滅した周期の後にバックオフ** | 0.25 → 2 秒。黙った ECU を全速で叩く意味が無い |
   | `--silent-max 60` | 沈黙が続いたら打ち切る |

   偽の ELM327（常に即 `NO DATA`）で実測: **3 秒で 630 万行 → 4 行**。
   1 時間換算で 75 億行 → 4,800 行（実際は 60 秒で打ち切るのでさらに少ない）。
   正常な ECU（1 要求 130ms）を模した検証では**修正前後で完全に同一**
   （1.91Hz / 1 周期 525ms）で、床は効いていない。

---

## 決定事項

### 2 本立ての位置づけ

**互いに上位互換ではない。敷居と得られるものが違う。**

- **Track A** — 市販 ELM327 で簡易的に実現する手段。ドングルと変換ケーブルだけで完結する
- **Track B** — 回路製作を伴い敷居は高いが、**市販ギアポジインジケータには無い高級ロガー**
  として使える

### Track A（ELM327 ＋ スマホ）

- 表示は計器のみ。回転・速度・ギヤの 3 段均等 ＋ 副表示 4 枠
- 取得は速い層 `0C`/`0D`/`11`、遅い層 `04`/`42`/`05`/`03` の持ち回り
- ギヤは**比推定**。理論減速比を軸に**単一のスケール係数 k** だけを当てはめる
- **較正はアプリ側で完結する。** `map.html` が CSV から k を自動較正し、`localStorage`
  経由で走行画面へ渡る（同一オリジン）。Mac の Python は詳細解析用として残す
- **`k` は実測でファームウェア定数と判明した**ので、当初想定していた「各自で較正が要る」は
  不要になった可能性が高い。既定値 1.013 のままで他の MC52 でも通るはず（要検証）

### Track B（ESP32 ＋ K-Line）

- **走行中の表示は 7 セグのみ。スマホはデータ転送専用。**
- リアルタイム性が要らずデータレートも遅くて困らないので、**スマホ自身のネット通信を
  阻害しない BLE** を選ぶ。参照実装は WiFi Station だが用途が違うので設計を分けてよい
- **BLE を選ぶならスループットを詰めること。** 6.7Hz・10 項目の CSV で 1 時間あたり約 1.5MB。
  既定のまま（MTU 23、1M PHY）だと 1〜2KB/s で転送に 12〜25 分かかり実用外。
  **MTU 拡張（247）＋ 2M PHY** で 20〜50KB/s、30 秒〜1 分になる。ESP32-S3 は両方使える
- ~~転送専用なので**走行中は BLE を広告しない**設計にできる~~ → **撤回。**
  ヘルメット内 HUD へ走行データを流す構成を想定するため、**走行中も BLE を
  張りっぱなしにする**。この基板はペリフェラルとして流すだけで、HUD 側が
  こちらにセントラル・スマホにペリフェラルというマルチロールを担う。
  **ハードウェアの変更は要らない**（2026-09-03 に確認）
- 代替として **USB MSC**（TinyUSB で MicroSD をそのままスマホに見せる）がある。スマホ側の
  コードがゼロで MB/s 出る。ケーブルと OTG が要る
- **差別化は表示ではなくデータ。** 噴射時間が取れる（標準 OBD 層には無い。`5E` 燃料流量も
  非対応を確認済み）。その積分は燃費そのもので、市販品はどれもやっていない
- **L9637D はロジック 5V、ESP32 は 3.3V。** RX 側にレベルシフトが要る
- 参照実装は **GPLv3**。プロトコルの事実を参照するだけなら制約は無いが、コードを流用すると
  本リポジトリも GPLv3 になる

### Track B の構成（2026-08-23 時点の決定）

**バイクに載せる以上、防水が効く。密閉を最優先し、開口部を潰す方向で設計する。**

| | 決定 | 理由 |
|---|---|---|
| ボード | **ESP32-S3 16MB**（N16R8 等） | 容量差の単価は小さく、ここを削る理由が無い |
| ストレージ | **内蔵フラッシュのみ。MicroSD は使わない** | SD スロットは実質的に開口部で、完全密閉と両立しない |
| 記録形式 | **バイナリ追記**（rpm/噴射時間/時刻差を u16、他を u8） | CSV の 1.38MB/時 に対し 0.32MB/時。16MB で約 40 時間 |
| 転送 | **BLE**。MTU 拡張 ＋ 2M PHY 必須 | バイナリ化で転送時間も同時に解決する |
| 操作 | **ボタン無し。設定は BLE 経由** | 押しボタンのシールが不要になり、開口部が減る |
| 表示 | **SSD1306 を第一候補。最終判断は日向で見てから** | `shared/libraries/SSD1306Ascii` が既にあり最短で動く |
| ファーム更新 | **標準の 2 面 OTA** | ESP-IDF / Arduino の組み込み機能。片面にすると電断で文鎮化し、密閉下では開封を意味する |

**表示器は日向での視認性が本当の制約。** OLED は自発光ゆえに直射日光が苦手で、7 セグ LED が
最も強い（市販ギアポジ製品が採用する理由）。情報量とのトレードオフになるため、**表示レイヤを
関数越しに抽象化**しておき、ST7789 IPS TFT 等へ差し替えられる形にする。残る開口部は表示窓のみ。

#### 記録の書き込みタイミング

**4KB たまるか 5 秒経過で周期追記し、加えて車速ゼロでフラッシュ＋クローズする。**

車速ゼロは確実にキーオフに先行する（走行中に施錠は起きない）ので、フラッシュ点として有効。
ただし**それ単独では 2 つ穴が残る** —— 高速道路で止まらない場合に RAM が保たないことと、
転倒や振動による瞬断が車速ゼロを経由しないこと。後者は一番記録が欲しい場面でもある。

周期追記のコストは実質ゼロ。フラッシュ寿命は無視できる範囲で、**LittleFS は電断耐性が設計
目標そのもの**（コピーオンライト）なので「途中で書くと危ない」という前提が成り立たない。

#### 基板製作

**JLCPCB で基板製作と SMD 実装まで発注し、表示器・端子のみ手ハンダする。**

- **最初に確認すべきは L9637D の在庫。** ST の古い車載向け部品で、ライブラリに無い可能性がある。
  無ければ**ディスクリートで代替できる**（K-Line は 12V プルアップの単線オープンドレインなので、
  TX は MOSFET でプルダウン、RX は分圧＋コンパレータ）。Basic 部品だけで完結し調達リスクが消える
- ESP32-S3 モジュールはほぼ Extended 部品。手配料を見込む
- **プログラミング用テストパッド（TX/RX/EN/IO0/GND）を必ず基板に置く。** 密閉後は USB が
  挿せないので、これを忘れると組み立て後に詰む
- SMD は片面に寄せる（両面実装は費用が上がる）。手ハンダ部品は反対面へ
- 12V 入力の保護（逆接保護・TVS・3.3V 降圧）も SMD 側

#### Track B が必要とする定数は Track A が供給する

- **真の車速** — `0D` に掛ける補正係数。ESP32 側に GPS は無いので固定値になる。係数は
  Track A の実走ログでの `0D`/GPS 突き合わせから決まる
- **残燃料推定** — 独自層の噴射時間を積分する（`2F` 燃料残量は非対応を確認済み）。
  **難所は表示ではなくインジェクタ流量定数の較正。** 満タンから次の給油までの積分値と実際の
  給油量を突き合わせ、タンク数本ぶん繰り返して確定させる。走っていれば溜まる

### 合流点

**ログ形式のみ。** 両方が同じ形式を吐けば `map.html` と解析ツールが共通で使える。
リアルタイムで合流させる必要は無い。

### 位置情報の扱い

生の軌跡・GPS ログは `private/logs/`（git 追跡外）へ。リポジトリに残すのは解析結果のみ。
車速の時系列も経路推定の手がかりになるため GPS と扱いを揃える。詳細は README。

**例示用の切り出しも公開しない。** `samples/` に法定速度内かつ日時をずらした区間を置いて
あるが、緯度経度は残るので ignore してある。**公開するのはスクリーンショットだけ**。
`tools/cutlog.py` と `tools/shifttime.py` があれば元ログから再生成できるので、成果物を
追跡する必要が無い。未公開の文章（みんカラ投稿の文案）は `drafts/` で同じく ignore。

---

## 次にやること

### Track A — ほぼ完成。残るのは細部

計測系は落ち着いた。欠測 1.9%、ギヤ確定 94%、レートは K-Line の予算いっぱい。
**大きな未解決は無い。**

手を付けるとすれば、

- **`tools/logger.py` の空転修正**（下記「既知の不具合」）
- 判定窓の調整。現状 ±4%＋量子化ぶんで確定 94%、残りは半クラと変速過渡で原理的に取れない
- 燃費推定。`0B` は脈動、`04` は空気量比で、どちらも単独では使えない。満タン法での較正が要る

### Track B — 回路図と部品配置まで進んだ（2026-09-02）

**[`hardware/README.md`](hardware/README.md) に分離した。** 要点のみ。

- **L9637D の在庫確認は済み。** `C153038`（E-L9637D013TR / SOIC-8 / 在庫 2,173 /
  $1.53@10）。**ディスクリート代替は不要**になった
- **BOM 27 点 19 品種、全点在庫あり、部品代 $8.32/台。** JLCPCB の発注 API から取得
  （当時ここに「28 点 18 品種」と書いていたが**数え違い**。部品代が同じなので BOM 自体は
  同一。`J3` を含めて 19 品種 27 点、EasyEDA の書き出しは `J3` が出ないので 18 品種 26 点）
- **電源を 1 系統に変えた。** L9637D の `VCC` は min 3V、TX の H 判定は min 2.5V、
  内蔵プルアップは `VCC` 行きなので、`VCC = 3.3V` なら 5V レールもレベルシフトも
  要らない。ただしデータシートは「特性は 5V でのみ試験」としているので**実機確認まで
  確定にしない**
- 降圧は AP63203WU-7（同期整流・内部補償）。TVS は SMBJ18A（クランプ 29.2V が
  降圧 IC の 32V 耐圧の内側）
- **回路図 28 部品 19 ネット、ピン単位で検証済み**（短絡 5 箇所を発見・修正）
- PCB は部品配置・外形 50×40mm・アンテナ禁止区域・`J3` 圧入穴まで完了。**配線が残っている**
- 表示器は未決のまま基板を確定させるため、`J2` を 6 極にして I2C / SPI どちらでも
  挿せるようにした。`J3`（書き込み）は部品を載せず**圧入用スルーホール 5 個**
  （SparkFun のジグザグ、1 穴おきに ±5mil）

作業は EasyEDA Pro ＋ 公式スキル `easyeda/easyeda-api-skill` で行った。**ドキュメントに
あるのに実行時に無い API が複数ある**（`autoRouting` `autoLayout` `pcb_Drc.check`）。
`importChanges()`（回路図→PCB 転送）も `true` を返すだけで効かないため、**PCB 側の
部品とパッドを同じネット定義から直接生成**して回避した。詳細は `hardware/README.md`。

生成スクリプトは `work/easyeda/`（リポジトリ外）:
`build_sch.py` 回路図 / `build_pcb.py` PCB / `layout_pcb.py` 配置 /
`export_mfg.py` 製造データ / `jlcstock.py` 在庫照会 / `make_bom.py` BOM。

### Track B — 配線が通った（2026-09-06）

**人手の介在なしで配線まで到達した。** 「GUI でしかできない」としていた 4 つのうち
3 つは私の判断誤りで、残る 1 つも不要だった。

| 手順 | 2026-09-03 の判断 | 実際 |
|---|---|---|
| DRC | API に無い | **`pcb_Drc.check(true,false,true)` で完全な結果が返る。記録が誤り** |
| 自動配線 | GUI でしか押せない | `getDsnFile()` → FreeRouting → `importAutoRouteSesFile()` で迂回 |
| 配線の消去 | — | API では不可。**`Route → Unroute → All` を DOM のクリックで叩く** |
| PCB 作り直し＋Import Changes | `NaN` を避けるため必須 | **不要だった** |

キーイベントは canvas に届かないが、マウス系はメニューに届く。これが鍵。

**配線の結果**: 配線 303 本 / ビア 24 個。ネット付き 90 パッド全部に配線が届いている
（自前の幾何照合、許容 0.5mil）。分かれているのは `GND` が 2 塊だけで、ベタパターンを
入れれば閉じる。

**配線前に見つかった実在の不具合 3 件**（詳細は `hardware/README.md`）

1. `C2_2`–`R8_2` の間隔が 5.68mil（要 5.98）→ `R8` を 12mil 逃がした
2. **`J3` の圧入穴 5 個と取付穴 4 個が丸ごと欠落**していた。このまま作れば書き込み
   ヘッダが 1 本も繋がらない基板になっていた
3. **アンテナ禁止区域が U2 の真ん中に置かれていた。** アンテナ下を守らず、U2 自身の
   パッド 10 個を潰していた。`createComplexPolygon` の矩形は y が上端という取り違えが原因。
   直した途端に未配線が 10 → 1 になった

道具は `work/easyeda/`（リポジトリ外）: `route_round.py` が往復一式、
`export_big.py` が製造データの書き出し（**発注 API は呼ばない**）、`menu.py` が DOM 経由の
メニュー操作。FreeRouting v2.4.1 と OpenJDK は導入済み。

**未了**: GND のベタパターンを入れると DRC が返らなくなる。切り分け中。
5V 退避用の分圧は**不要として取り下げた**（`VCC` 全範囲は設計保証）。
`C12` の Basic 化は**次版へ送る**。**発注は触っていない。**

### Track B — 発注した（2026-09-07）

**JLCPCB へ PCBA を発注した。** 設計・製造データ・検証はすべて確定済み。

| 項目 | 内容 |
|---|---|
| 基板 | 50.00 × 40.00mm、2 層。ツーリングホールは JLCPCB が追加する |
| 実装 | Standard PCBA、片面（Top）。**ESP32 が Economic で選べない**ため Standard |
| 数量 | **実装 2 枚。** 基板の最小が 5 枚なので**素板が 3 枚余分に届く**（ケースの現物合わせに使う） |
| 部品 | 16 品種 24 点。`J1` `J2` `J3` は実装対象から外して手ハンダ・圧入 |

**`部品配置の確認` は指摘なしで通った。** `Show Revised Items` が空、つまり
**JLCPCB は送った実装座標を一切変更していない**。DFM の指摘も無し。
実装に使われるのは `hardware/jlc_cpl.csv` そのもの。

なお確認画面のレンダで基板が回って見えることがあるが、**外形と座標は同じ座標系から
出ているので一様な回転は組み立てに影響しない**。判定するなら外形が 50 × 40mm＝
縦横比 1.25 で正方形でないこと、コネクタ `J1` `J2` を下辺に置いたとき
`U2`（ESP32）が右・`U1` `U3` が左に来ることを見る（左右が逆なら裏面ビュー）。

#### 届いたらやること

**PCBA なので `U1`（降圧）・`U2`（ESP32）・`U3`（L9637D）を含む 24 点すべて実装済みで届く。**
手を付けるのは `J1` `J2` のコネクタだけ（`J3` は圧入ヘッダ用の穴で BOM に無い）。
**電源部だけを切り離して通電することはできない** —— VIN を入れた瞬間に全部が同時に起きる。
余る素板 3 枚は本当に何も載っていないので、ケースの現物合わせ専用。電気的な確認には使えない。

**バイクは要らない。** 基板に `R7`（510Ω・`VIN`→`KLINE`）が載っていて、これが K ラインの
プルアップ。`J1` の `KLINE` を**未接続のまま** 12V を入れれば K は 12V に吊られ、`KL_TX` を
Low にすれば L9637D が K を引き下げて `KL_RX` に返る。**TX→K→RX が基板内で閉じる。**
`U3` は pin3=`3V3`（ロジック VCC）/ pin7=`VIN`（バス側 VS）なので、**懸案の「VCC 3〜7V だが
試験は 5V のみ」はまさにこのロジック側**で、机上で判定できる。

1. **通電前に VIN–GND / 3V3–GND の抵抗を当てる。** ショートが無いことだけは先に確かめられる
2. **電流制限付きで 12V を入れ、電流を見ながら 3V3 を測る。** ESP32 は書き込み前なので
   起動に失敗して UART にエラーを吐き続けるはず。それが見えればレールと ESP32 は生きている
3. **`J3` の圧入ヘッダを挿して書き込む。** 密閉後は USB が使えないのでここで確認しておく
4. **`firmware` の `selftest` を焼いて机上で測る**（`pio run -e selftest -t upload`）。
   バイク不要。**`HANDOFF` に数字だけ書いて一度も測っていない主張 3 つをここで全部潰す**

   | | 測るもの | 主張 |
   |---|---|---|
   | `k` | **K ライン折り返し** | L9637D が VCC=3.3V で通るか。DC → 10400bps の誤り数 → 速度余裕 |
   | `f` | LittleFS | バイナリ 0.32MB/時・約 40 時間 |
   | `b` | BLE | MTU 247 ＋ 2M PHY で 20〜50KB/s（既定は 1〜2KB/s で実用外） |

5. **ここまで通ってから車両に繋ぎ、標準 OBD 層を読む。** Track A で対応 PID も
   期待値も実測済みなので、**ファームのバグと車両の未知を切り分けられる唯一の足場**になる
6. **Honda 独自層を探索する。これが本命。** ELM327 では原理的に叩けない層で、
   **この基板が必要になった理由そのもの**。標準層は目的ではなく足場にすぎない
   （※ 当時は `obd` / `probe` の 2 環境だったが、後に `explore` 1 本に畳んだ）

   | | |
   |---|---|
   | `s` | **テーブル `0x00`〜`0xFF` 総当たり。MC52 で最初にやること** |
   | `d` | `0xD1` の index 4 = 噛み合い状態。**比推定に欠けていた入力**で、クラッチ切りの誤判定を根本から消せる |
   | `m` | `0x11` / `0x10` のセンサ群と **index 18-19 の噴射時間**。標準層に無く、積分すれば燃費そのもの |

   **MC52 が喋ること自体が未検証。** ウェイクアップは CRF250L と CBR600RR で既に
   違っており、MC52 がどちらでもない可能性がある。仕様の出所と信頼度は
   `firmware/PROVENANCE.md`（収集は別系統、受け渡しは文書 1 本、全フレームを自前で検算）
7. 余った素板 3 枚でケースの現物合わせ

**次版へ送った項目は `hardware/README.md` に集約してある**（`SW` ノードの再配置、
GND ベタ、`C12`・`C1` の Basic 化、バックの熱計算、表示器確定後の FSPI IOMUX 整合）。

### Track B — 机上試験を通した（2026-09-15）

**基板が届き、バイクに繋がずに確かめられることは全部終わった。**
`HANDOFF` に数字だけ書いて一度も測っていなかった主張 3 つが、すべて実測で裏付けられた。

| | 主張 | 実測 | 判定 |
|---|---|---|---|
| **L9637D を VCC=3.3V で使えるか** | データシートは 3〜7V を設計保証、ただし**試験は 5V のみ** | **10400bps で 1024 バイト誤り 0。**19.2k / 38.4k / 57.6k / **115.2k まで誤り 0** | **余裕 11 倍。ぎりぎりではない** |
| 記録容量 | バイナリ 0.32MB/時、16MB で約 40 時間 | 空き 11.9MB → **37 時間**。書き込み 202 KB/s（4KB ごと flush）、読み出し 2753 KB/s、不一致 0 | 一致 |
| BLE スループット | MTU 拡張＋2M PHY で 20〜50KB/s（既定のままなら 1〜2KB/s で実用外） | MTU 247 で交渉成立、パケット長 244、**33.2 KB/s**。1 時間ぶんの転送が **10 秒** | 見積もりの上側 |

記録に要る速度は 0.32MB/時 ＝ **91 バイト/秒**。フラッシュは 2000 倍の余裕がある。

#### 電源部（テスタのみ）

| | 測定 |
|---|---|
| 12V 源 | 12.27V |
| `C2` 左足（VIN、`D1` の後） | 12.00V。**差 0.27V = SS36 の順方向降下**で、向きも正しい |
| `J2` 1–2（3V3） | **3.314V**。AP63203 の公差 ±1.5%（3.25〜3.35V）のど真ん中 |

**順方向降下は電流計の代わりになる。** SS36 は 0.1A で約 0.35V なので、0.27V は 10〜30mA 相当。
想定していたアイドル約 20mA と合う。アンペア級を引いていれば 0.5V を超える。

#### バイクが要らなかった

基板に **`R7`（510Ω・`VIN`→`KLINE`）** が載っているため、`J1` の `KLINE` を未接続のまま
12V を入れれば K は 12V に吊られ、**TX→K→RX が基板内で閉じる**。`U3` は pin3=`3V3`
（ロジック VCC）/ pin7=`VIN`（バス側 VS）なので、懸案の「VCC 3〜7V だが試験は 5V のみ」は
まさにロジック側で、**机上で判定できた。**

**これで 5V 2 段＋レベルシフトから 3.3V 1 系統へ変更した判断が実証された。**

#### 途中で見つけた不具合

- **パーティション名は `spiffs` でなければならない。** 中身が LittleFS でも、Arduino の
  `LittleFS.begin()` も PlatformIO の `uploadfs` も**既定でラベル `spiffs` を探す**。
  `littlefs` という名前にしたら `partition "spiffs" could not be found` で落ちた。修正済み
- 未フォーマットのパーティションを初回マウントすると `Corrupted dir pair` が出るが、
  これは正常。`begin(true)` がその場でフォーマットして成功する

#### 手順上の注意

- **自動リセットは繋いでいない**ので、書き込みモードへは手で入れる。
  `IO0` を GND → `EN` を一瞬 GND → `EN` 開放 → `IO0` 開放。
  **`EN` 開放の瞬間に `IO0` が GND に落ちていること**が全て
- **「出力が静止している」はダウンロードモードの判定に使えない。** フラッシュが空のうちは
  ROM が `invalid header` を吐き続けるので静止＝成功だったが、ファームを焼いた後は
  起動しても静止する（コマンド待ち）。**`h` を送ってメニューが返らないこと**で判定する
- UM232R はジャンパ `J1` を **1–2 で 3.3V**（2–3 は 5V で基板を壊す）

#### 次

**残るは車両に繋いでからの 2 つだけ。**

1. `explore` の `o` で標準 OBD 層（足場。Track A と一致すればファームの土台が正しい）
2. **同じ `explore` の `w` / `s` で独自層。これが本命**

（当初は `obd` と `probe` の 2 環境に分けていたが、現場で焼き直さずに往復できる
ことが切り分けの速さに直結するため `explore` 1 本に畳んだ。下の節を参照）

### Track B — 探索の準備が整った（2026-09-15）

**ハーネスを作り、OTA を通した。以降 `J3` のジャンパ操作は要らない。**

#### 4 ピンカプラ → `J1` の配線

| 4pin（住友 6187-4441） | 信号 | → |
|---|---|---|
| 1 | GND | `J1`-2 |
| 2 | **SCS** | **使わない**（GND に落とすと ECU が自己診断モードに入る） |
| 3 | +12V（IG 連動） | `J1`-1 |
| 4 | K-Line | `J1`-3 |

**車体側と勘合する相手カプラは、市販の「Honda 4pin → OBD2 16pin」変換ケーブルを
切って流用した。単体では入手しにくいため。** Track A で使っているものと同じ物で、
1 本余分に買えばいい。

ピンヘッダに線をハンダ付けし、個別に熱収縮で固めてから基板へ実装した。
**ピンがスルーホールに入るので保持が線直付けより桁違いに強い。** 樹脂スペーサが
熱と応力のブレークになる。

**ハーネスの検証はバイクへ行く前に済ませられる。** カプラを車両に挿さなければ
K 線は開放端なので、`R7` のプルアップで折り返しが成立する。`k` を回して

```
DC        TX=H→RX=1 / TX=L→RX=0     経路成立
10400bps  1024 バイト 誤り 0        健全
```

が出れば入れ違いも短絡も無い。**論理が反転していれば VBAT と KLINE の入れ違い、
エコーに誤りが出れば K 線が GND か 12V に落ちている。**

#### OTA

**WiFi OTA を入れた。**`pio run -e explore_ota -t upload --upload-port <IP>` で飛ぶ。
実測で 1.37MB を 48 秒。

- **SSID とパスワードはソースに書かない。** このリポジトリは PUBLIC。BLE で
  `wifi <ssid> <pass>` を打って NVS に保存する
- **最大 4 件登録でき、`WiFiMulti` が見えているものを自動で選ぶ。** 家の WiFi と
  スマホのテザリングを両方入れておけば、場所で設定を変えなくていい
- **既定で切。** `ota on` で明示的に有効化し、再起動で戻る。LAN に無認証の
  書き込み口を開き続けない
- **`J3` は最後の逃げ道として残す。** 壊れたファームを飛ばすと BLE も OTA も死ぬ

mDNS で `mc52-explore.local` として見える。

#### 手順上の落とし穴

- **「出力が静止している」でダウンロードモードを判定できない。** フラッシュが空の
  うちは ROM が `invalid header` を吐き続けるので静止＝成功だったが、ファームを
  焼いた後は起動しても静止する。**`?` を送ってメニューが返らないこと**で判定する
- **macOS は BLE の名前をキャッシュする。** ファームを入れ替えても古い名前を返し
  続けるので、**サービス UUID で探す**（`klconsole.py` はそうしてある）
- **notify は UTF-8 の途中で切れる。** 受け側を逐次デコーダにしないと日本語が化ける
- ヘルプに**ビルド時刻**を出すようにした。どの版が載っているか迷わないため

### Track B — 記録と吸い出しを作った（2026-09-16）

**金曜の探索結果に依存せず、実用フェーズに必ず要る部分。** 机上で完結して検証
できるので先に作った。実機で端から端まで通してある。

```
rec on → s(256 要求) → rec off → ls → get → 復号
2327 バイト  CRC 86702689 一致
```

記録形式は**生フレームの追記**。テーブルのバイト配置も噴射時間の単位も未確定なので、
解釈して書くと間違いが確定した時点で過去のログが全部無価値になる。詳細は
[`firmware/README.md`](firmware/README.md)、道具は `tools/kllog.py`（吸い出し・CRC 検証）、
`tools/kldecode.py`（レコード分解・チェックサム検算）、`tools/klwatch.py`（`poll` の
実時間監視。**動いたバイトを探す計器**）。

**記録点は `xfer` の 1 箇所だけ。** 送受信の唯一の通り道なので、要求も応答も自動で残る。

`tools/klota.py` を足した。OTA は**既定で切・再起動で戻る**設計なので、開発中は毎回
BLE で `ota on` を打つことになる。有効化から書き込みまでをまとめたもの。

**実機で見つけた不具合 2 件**（どちらも動かして初めて出た）

- `rec on <名前>` の `on` を名前として食っていた（`on bench.bin` が出来た）
- `kllog.py` が転送の `BEGIN` 行のサイズを固定位置で取っていた。名前に空白が
  入ると壊れるので末尾から取る

おまけで、最初の要求 `72 05 71 00 18` が**仕様書の keep-alive フレームと完全一致**した。
チェックサム実装の裏が取れている。

### Track B — 実時間ストリームと文書の整理（2026-09-16〜17）

**`poll` を足した。指定したテーブルを周期読みし、解釈せずに生バイトを押し出す。**

`d` や `m` は 1 回読むだけなので、**クラッチを握った瞬間にどのバイトが動くか**が
取れない。バイトの同定は操作と変化の対応でしか進まないので、時系列が要る。
解釈を含まないため、**先行事例のインデックスが MC52 で外れていてもこの層は
間違いにならず**、実用フェーズの送信経路にそのまま使える。

```
P <経過ms> <TT> <hex..>      応答       ← 人向けの日本語出力とは混ぜない
P <経過ms> <TT> -            無応答
```

受け手は `tools/klwatch.py`。**表示項目を決めない表示**にした —— バイトを格子で
並べ、動いたバイトを強調し、出すのは現在値・**種類（そのバイトが取った異なる値の
数）**・スパークラインの 3 つだけ。**種類 1 なら定数、2 なら旗、多いなら量。**
画面下の行は基板へ素通しするので `note` をその場で打てる。

**無応答のほうが遅いことが実測で分かった。** 応答すれば 1 テーブル 60〜85ms だが、
応答しないテーブルは待ち 200ms を使い切る。机の上（ECU なし）で **2 本 2.04Hz /
1 本 3.3Hz**。**帰結: `s` で実在するテーブルに絞ってから `poll` に渡す。**
存在しないものを 1 本混ぜるだけで 200ms を捨てる。

通しの確認も取った。OTA 41 秒、`P` 行受信、`rec` 併用（`ptest.bin` 306 バイト =
ヘッダ 9B ＋ 要求レコード 9B × 33 周期と桁まで一致）、`poll off` で停止。
**BLE は 1 本しか張れないので `klconsole` と `klwatch` は排他。**

**実装仕様書 [`firmware/SPEC.md`](firmware/SPEC.md) を起こした。** いま何が実装
されているかをコードから起こしたもので、**実装仕様**（コードを読めば確かめられる）と
**プロトコルの前提**（先行事例からの推定。MC52 では未検証）を混ぜずに分けてある。
図は mermaid にしたので GitHub がそのまま描く。

そこで整理して分かった一番効く事実が**状態の寿命**。IG 連動でキーを切るたびに
ブートし、**`rec` の入切・`poll` の設定・OTA・壁時計は全部 RAM で消える。**
NVS に残るのは WiFi 資格情報、LittleFS に残るのはログだけ。
**帰結: エンジン始動 → BLE 接続 → `rec on` の順で固定。**

**`J2` に OLED を挿すときの罠を `hardware/README.md` に書いた。** 常設はしない
（`J2` は 1 本しかなく走行時の表示器と争う、ケースに窓が無い、静止表示で焼き付く）。
作業中だけ挿す計測器として扱う。罠は 4 つ —— **市販モジュールは電源の並びが逆**、
`Wire.begin(10, 9)` は `J2` の物理順と逆、プルアップは基板側にある（`R3` `R4`）、
**全面転送は 400kHz で 23ms・既定の 100kHz では 92ms** かかるので ISO 14230 の
P1 を割らないよう要求の合間に更新する。

**文書の置き場を整理した。**

| | |
|---|---|
| ルート | 9 → **6 ファイル**。`FINDINGS.md`（1007 行）と `GEARING.md`（460 行）を `docs/` へ移した（`git mv`） |
| 動かさなかったもの | `index.html` / `map.html`（**Pages の配信元が `/`**）、`README.md`（トップ表示）、`HANDOFF.md`（資料でなく作業の入口） |
| 消したもの | `pcb_state.json`（中身が `{}` の空ファイル）。gitignore に入れた |
| 検証 | 追跡下の md / html の**相対リンク 68 本を機械的に解決**して切れ 0 を確認 |

**`hardware/README.md` が製造前のままだった。** 「配線が残っている / ガーバーは
発注不可 / 実機確認もしていない」と書いてあった。実際に合わせたうえで、数の
食い違いも一次データで決着させた —— **27 部品 / 18 ネット**（`netlist.md` の
ネット節 18 と `bom.csv` の数量計 27 が一致）、**BOM は 19 品種 27 点で、EasyEDA
出力の 18 品種 26 点との差は `J3` だけ**（部品を載せないので出ない）。
実在しないファイルを指すリンク 3 本も直した（`netlist.enet` → `Net_List.enet`、
`bom_easyeda.tsv` → `Export_BOM.csv`、`cpl.tsv` → `Pick_Place` / `jlc_cpl.csv`）。

**スマホアプリは作らない。** 板を第 2 トランスポートにして走行監視をスマホへ、
という案は出たが、**表示項目がまだ決まらない**（バイト配置が未知）。項目に依存
しない部分だけ作ると生バイトを流す端末になり、それは `klwatch` と同じ物。
解析は Mac で足りるので、`index.html`（ELM327）＝走行中の道具、板＝Mac に繋いで
使う解析機材、という分担で確定した。

### Track B — 実車で独自層を掴んだ（2026-09-17、停車）

**独自層は応答した。しかも IG ON（エンジン停止）でも喋る。** ウェイクアップは
**A（`FE 04 72 8C`、CRF250L 系）**で通り、CBR 系は使わずに済んだ。実測の全量は
[`docs/FINDINGS.md`](docs/FINDINGS.md) の「実車で叩いた結果」。

**裏付いた前提** —— 70ms ブレーク / 初期化 `72 05 00 F0 99` / テーブル読み
`72 05 71 TT CS` / 応答の先頭 `0x02` / 2 バイト目 = 長さ / ボーレート 10400、そして
**チェックサム = 総和の 2 の補数が走査 256 フレームで 256/256 一致**。

**実在するテーブルは 7 本**（`00` `11` `20` `61` `70` `D0` `D1`）。249 本は長さ 5 の
空応答。**`0x10` は存在しない**（CBR600RR のテーブル）。**`0x61` は凍結データ** ——
25 分・暖機の進行をまたいで 152 サンプル全部バイト同一で、`0x11` と同じ 25 バイト長だが
中身は生きていない。

**`0x11` のセンサ配置が当たった。** アイドリングで RPM 1428 / ECT 44℃ / IAT 28℃ /
電圧 14.5V / 車速 0。噴射時間 [18-19] はアイドル 611〜648 で安定し、煽ると 801、
**閉じた瞬間に 370 まで落ちる**（燃料カット）。MAP [12-13] がアイドルで 30〜97kPa を
往復するのは単気筒の吸気脈動で、標準層の `0B` と同じ挙動。

**狙っていた観測量が手に入った。`0xD1` の index 4。**

| | |
|---|---|
| `00` | 駆動が繋がっている（ギヤが入り、クラッチも繋がっている） |
| `01` | 繋がっていない（N **または** クラッチを切っている） |
| `03` | サイドスタンドが出ている |

**減速中のクラッチ切りは `01`、真の 6 速は `00`。** 標準 OBD 層では原理的に弾けなかった
誤判定（28km/h・1959rpm が真の 6 速と数値的に同一）が、1 バイトで分かれる。

**一度「クラッチは見えない・誤判定は潰せない」と誤結論した。** 状態を
A（スタンド出す・N）→ B（スタンド払う）→ C（クラッチ握る）→ D（1 速・クラッチ握る）と
作って、D で index 4 が `01` のままだったことから棄却へ進んだ。**誤りの原因は D を
クラッチ握りで作ったこと** —— 足りていなかったのは「ギヤが入って、かつクラッチが
繋がっている」状態で、それは停車では作れないと自分で書いておきながら穴を埋めずに
結論した。**エンジンを切って車体を手で転がしたら作れた**（index 4 = `00` が 0.2〜0.8 秒の
バーストで 10 回現れた）。

> **教訓: 「その状態は作れない」と判断した時点で棄却の結論は出せない。**
> 作れない状態が真偽を決めるなら、作る方法を先に考える。

**手順に効く実測が 3 つ。**

- **keep-alive は要らない。** `ka 0` で 5 / 10 / 20 / 40 / 60 秒の無通信を挟んでも
  5 回すべてテーブルが読めた。仕様書の「`0x00` を 2 秒周期」は MC52 では不要で、
  その往復を実データに回せる（60 秒超のタイムアウトは未測定）
- **`w` は電源投入後に必須。** `w` の前のテーブル読みは `FF` 1 バイトしか返さない
- **標準層と独自層はセッションとして排他。** `w` の後に `o` を打つと無応答。
  電源を入れ直して `w` の前に打つと通る（`0C` = 1436rpm、`0D` = 0km/h）。
  **つまりこの基板は ELM327 の代わりにもなる**

**物理層は健全。** 車両に挿した状態で `X 72 05 71 D1` を 5 回、**エコー誤り 0**。
`poll D1 11` の実効レートは**実車で 5.0Hz**（749 周期 / 150 秒）。

**`get` の転送は通知落ちする。** `probe.bin` の 1 回目は 117086 バイト中 116816 しか
届かず CRC 不一致、2 回目で一致。**CRC を付けてあるので黙って壊れたファイルを掴まない。**

回収したログ（`private/logs/`、git 管理外）:

| | | |
|---|---:|---|
| `probe.bin` | 117086B / 7979 レコード | 走査・空吹かし・状態 A〜F |
| `gearpos.bin` | 18126B / 1510 | ギヤ掃引（エンジン停止） |
| `final.bin` | 551B / 36 | 標準層・keep-alive 測定 |

#### 停車探索の手順（2026-09-17 に実施済み）

**実施済み。** 停車・エンジン掛けで完結するという想定は正しく、走行が要るのは車速バイトの
同定・ギヤ位置の有無・噛み合いフラグの追従確認だけだった。以下は実施した手順の記録で、
他車種（THS 等）へ横展開するときの型としても使う。

実際にやってみて分かった補正が 3 つある。

- **`o`（標準層）は `w` より先に打つ。** 後に打つとセッション排他で無応答になる
- **`rec on` は始動の後。** RAM なのでキーを切ると消える
- **`s` の結果で実在するテーブルに絞ってから `poll` に渡す。** 存在しないテーブルは
  無応答の待ち 200ms を使い切るので、1 本混ぜるだけで周期を捨てる

```
.venv/bin/python tools/klconsole.py
> !log ../work/mc52-probe-<日付>.txt     こちら側のテキスト記録
                                          壁時計は接続時に自動で入る
> rec on probe                            基板側に生バイトを残す
  … 下の 1〜5 …
> rec off
```

戻ってから `.venv/bin/python tools/kllog.py get --all` で吸い出す。
**`!log` はテキスト、`rec` は生バイト。** 後者は何度でも解釈し直せる。

1. **`o`** 標準層。Track A と同じ値が返れば土台が正しいと確定する
2. **`w`** 独自層の初期化（ウェイクアップ 2 種を自動で試す）
3. **`s`** テーブル総当たり。**MC52 が何を持っているか**。ここまでで応答する
   テーブル番号が確定するので、以降はそれを `klwatch` に渡す

**4 と 5 は `klwatch.py` に持ち替える。** スナップショットでは操作と変化の対応が
取れない。**BLE は 1 本しか張れないので、`klconsole` を `!q` で抜けてから**起こす。

```
> !q
.venv/bin/python tools/klwatch.py --tables D1 11 --ms 200 --rec probe
```

動いたバイトが強調され、`種類`（そのバイトが取った異なる値の数）が出る。
**種類 1 は定数、2 は旗、多ければ量。** 画面下の行はそのまま基板へ渡るので、
操作のたびに `note` を打って後から突き合わせる。

4. **空吹かししながら** RPM / TPS / MAP の位置と**噴射時間（index 18-19）の桁**。
   スロットルに同期して動くバイトが、そのまま同定結果になる
5. **ギヤを入れてクラッチを握る／離す。** 先週潰せなかった誤判定の解が成立するかは
   この 1 点で決まる。`0xD1` の index 4 が `00`⇄`01` で追従するか、
   **それとも別の index が動くか**（先行事例は CRF250L / CBR600RR で世代が違う。
   位置が外れている可能性を最初から見る）

`w` が無反応なら `brk` と `X` で手探りに切り替える。**ブレーク長もウェイクアップの
バイト列もボーレートも、焼き直さずに振れる。**

### Track B — 実走で 4 つ全部決着した（2026-09-19、2 本）

**狙っていたものは全部取れた。** `poll D1 11 200` を `rec.html` から仕込んで走行。
生データは `private/logs/r260919_1150.bin`（8.9 分）と `r260919_1224.bin`（4.0 分）、
合わせて 13 分 / 3837 サンプル / **5.0Hz / テーブル**。実測の全量は
[`docs/FINDINGS.md`](docs/FINDINGS.md)「走行で決着したこと」、判定への波及は
[`docs/gear.md`](docs/gear.md)「真値と突き合わせた」。

| | 結果 |
|---|---|
| ① index 4 の追従 | **成立。** 駆動中は比が 90.0% 確定・誤差中央 2.48%、非伝達では 7.5% |
| ② 正解ラベル | **取れた。** 誤答を機械的に数えられるようになった |
| ③ index 9 が段数か | **段数ではない。** 3837 サンプルすべて `00`。実在 7 テーブルに段数は無い |
| ④ 車速バイトの校正 | **`0D` と同じ信号。** `[17]`/GPS は 30km/h 以上で中央 0.939（全体 0.9346）。予測 0.9333・標準層の実測 0.935〜0.942 と同じ範囲。**既存の校正を継承できる**（当初ここに書いた最小二乗 `0.9332×GPS − 0.216` は再現しない。[FINDINGS.md](docs/FINDINGS.md) の撤回節を参照） |

**②が最大の成果。** これまで誤判定は同じ 12 本のログに手でラベルを付けて数えており、
**過学習を検出できない**という弱点があった。index 4 は ECU が出す真値なので、
判定則の案を手作業なしに評価できる。

**「確定」1568 件のうち誤答 131 件 = 8.4%。ただし率は走行内容に強く依存する** ——
市街地中心の 1 本目 6.7%、シフトダウンを意図的に入れた 2 本目 12.1%。
**率を精度として書いてはいけない。** 2 本で交絡が実証された。

**比が原理的に解けない帯を index 4 が分けた。** 車速 20〜40km/h・rpm 1700〜2400 で、
駆動中は 75 点のうち 62 点が 6 速に集まり（一貫＝正しい）、非伝達では 18 点が
3/4/5/6 に散る（無意味）。同じ rpm・同じ車速でラベルの質が反転する。

**誤答の機構も特定した。** 低速では**量子化の余裕 `0.5/車速` が 8km/h で 5.9%** に
なり、1 速の上側の窓 4.0% と合わせて 9.9% まで許すため、誤差 6% の半クラが通る。
**正しい項が、正しい理由で、低速域を通しすぎている**ので窓の調整では直せない。
高い段では「回転を合わせたシフトダウン」が別の段の窓に入る。

**副産物**

- **index 8 はビットフィールドだった。** 停車では `01`/`05` の 2 値、走行で
  `0b1001` `0b1101` が出た。bit 3 は車速に対応、**bit 2 は未同定**（同じ速度・回転で
  噴射が 654 と 1176 に分かれるので回転しきい値では説明できない）
- **噴射 `[18-19]` が `0` になった。** 減速時の燃料カット。範囲 0〜1546、endian は BE
- `rec.html` が実走で機能した。基板の時計をページが入れたので**測位ログと同一時間軸**に
  なり、④の突合がそのまま成立した

#### 次

| | |
|---|---|
| **判定への実装** | `map.html`（ビュワー）は基板ログを読めば index 4 を使える。`index.html`（モニタ）は ELM327 経路なので**入れられない** —— index 4 は真値として判定則の評価に使う |
| 噴射の絶対単位 | 値 × 回転数の積分と**給油量**の突合。1 タンク分の記録が要る |
| **走行抵抗の実測** | **燃料カット（噴射=0）と `index 4` でコーストダウンを 2 種類に分けられる**（クラッチ切り＝空気＋転がり / ギヤ繋ぎのカット＝＋エンジンブレーキ）。`GEARING.md` が仮定で置いている CdA と Crr が `v²` 項と定数項として分離して求まる。**要るのは 60km/h 以上から数十秒の長い惰行**で、市街地の 2 本では区間が短く足りない |
| `0x20` / `0xD0` | **`poll D0 20 11 D1` で 4 本同時。各 5.0Hz を維持できる**（1 本 37ms、周期 200ms に 5 本まで入る） |
| 誤答率の交絡 | ログを増やす。率ではなく機構で語る |
| index 8 bit 2 | 未着手。**index 9 はラジエータファンと同定済み**（入 100℃ / 切 98℃） |

#### 基板の現状（2026-09-17 時点。下に最新あり）

| | |
|---|---|
| ファーム | `explore`（記録つき）。**OTA 済み** |
| WiFi | 自宅とテザリングの 2 件を NVS に登録済み。`WiFiMulti` が自動で選ぶ |
| BLE | `MC52-explore`。**macOS は名前をキャッシュするのでサービス UUID で探す** |
| LittleFS | 7 ファイル / 376KB（空き 11.8MB）。**実走 2 本を含め全部 `private/logs/` へ回収済み（CRC 一致）なので `rm` してよい** |
| ハーネス | 4 ピンカプラ直結。`k` で検証済み |
| ケース | 仮組みプレートで運用中。**本組みは M2 × 20 待ち** |
| `poll` / `klwatch` | **通しで確認済み**（2026-09-16）。OTA 41s、`P` 行受信、`rec` 併用、`poll off` まで。ECU が無いので全部無応答で **2 本 2.04Hz / 1 本 3.3Hz**（無応答は待ち 200ms を使い切るため。応答すれば速くなる） |

#### 基板経路のモニタを作った（2026-09-19）

**`rec.html` を基板経路のモニタにした。** UI は `index.html` の計器レイアウトを踏襲
（3 段を同じ高さ・回転バー・副表示 4 枠・状態ボタンは効力が続く間点灯・横向きで 3 列へ
組み替え）。副表示は `THR / INJ / ECT / VOLT` —— `LOAD` は独自層に無く、そもそも
トルクの指標にならないと確認済みなので枠を噴射時間に充てた。

**ギヤ判定は index 4 があるぶん ELM327 版から 3 つ落とせた** ——時刻ずれの補間（同一
フレームなので不要）、回転数 1,700rpm 下限（index 4 が担う）、判定窓（誤答を弾く役から
外れ、駆動中なら最近傍で決まる）。変速レート制限だけ残した（調整値を持たない）。

**UX は「キーを回すだけ」になった。**

```
IG ON → ブート → w → snap → rec on autoNNNN → poll D1 11    ← auto が全部やる
       ↓ エンジン始動。セッションが落ちても 3 秒後に基板が張り直す
スマホ接続 → 壁時計 ＋ note t=<epoch> のアンカー → 未転送を裏で回収 → モニタ表示
       ↓ 停車、エンジンを掛けたまま REC 停止 → 取込（CRC 一致）
キーオフ。転送しきれなければ切ってよい（次回接続時に裏で続く）
```

**初回接続は「基準」を取るだけにした。** Pixel で最初に繋いだとき、保持一覧が空だった
ので**基板にある 7 本（280KB）を一斉にダウンロードしてしまった**。Mac に既にあるものまで
スマホへ落ちてくるのは無駄なので、初回はそのとき基板にあったものを落とさずに既知として
登録し、**以後に現れた新しいログだけ**自動回収する。個別に欲しければコマンド欄に
`get <名前>` と打てば落ちてくる。

**容量の扱いを 3 段にした。** それまで `logFlush()` が `write()` の戻り値を見ておらず、
**満杯になると記録が静かに落ちていた**（自動記録では誰も気付かない）。

| | 契機 | 基準 |
|---|---|---|
| 開始時の確保 | `rec on` | 空き **4MB** = **1 走行ぶん**（18.5KB/分で約 3.7 時間） |
| 走行中の底上げ | 60 秒ごと | 空き **512KB**。長距離でも止まるより続く |
| 最後の砦 | `write()` が短い | 声を上げて記録を止める |

消す対象は `auto*` と `r*` だけ（名前順＝生成順、記録中のものは除外、一覧を `note` に残す）。
**最初は開始時の基準を 1MB にしていたが、18.5KB/分では約 57 分しか入らず「1 走行ぶん」に
届いていなかった。**

**退避は未回収の走行ログも食う。** 基板は何が回収済みかを知らないので判断は
「新しいデータの方が価値が高い」で固定。**通常の容量管理は「回収して `rm`」で、
退避は最後の砦**という位置づけ。詳細は [`firmware/SPEC.md`](firmware/SPEC.md) §14。

**ログを 256KB で分割した。** 背景転送は 1.3KB/s しか出ないので、1 本が大きいと接続した
走行の中で取り切れず退避に食われる。**部分データは受け手のメモリにしか無く取り直しは
先頭から**なので、それが痛くない大きさに切る。`<base>_PP.bin`、名前順＝生成順。
`dt` の連鎖はまたいでも切らない（パート境界の先頭 `dt` が実間隔。実測 260ms）。
**ヘッダ壁時計は最大 5 秒遅れる**ので連結は `dt` で行う。

**転送中に BLE が切れたら転送を捨てるようにした。** それまでは `getOn` が立ったままで、
`outBle()` が接続無しだと Serial へ落とすので**誰も受け取らないデータを吐き続けて
`poll` の取り分を食っていた**（1MB で約 170 秒）。

**実機で確認したもの** —— 転送の非ブロッキング化（`poll` 稼働中も `P` 行が 4.29 行/s）、
セッション復帰（40 秒に 4 回発火）、`note t=<epoch>` のアンカー（ログから拾って
ファイル先頭の絶対時刻を復元、ヘッダとの差 1 秒 = `time` と `rec on` の間隔ぶん）。
**未確認は Pixel で新 UI を開くところだけ。**

#### 3 本目 —— 自動記録の通し（2026-09-19、18.4 分）

**`auto` に任せた初めての走行。記録の連鎖は全部動いた**（`private/logs/auto0002_0*.bin`、
5493 行）。

```
auto seq=2 prop=ok       IG ON で独自層が起きた
t=1789798664（5.9 秒）   スマホ接続でアンカーが入った
roll: 次のパートへ        262144 バイト = 256KB ちょうどで切り替わった
reinit のメモ無し         **クランキングでセッションが落ちなかった**
```

**ヘッダの壁時計は `3`（未設定）で、アンカーだけが絶対時刻を与えた。** 自動記録の
時刻復元が実データで成立した。判定は駆動中 2982 行すべてに段が付き（ELM327 方式なら
96.3%）、**駆動していないのに段を答えた行は 0**（同 4.2% = 125 件）。誤答率は
1 本目 6.7% / 2 本目 12.1% / 3 本目 4.2% と**走行内容で 3 倍動く**。

**測位が走行と 1 秒も重ならなかった。** `auto` 任せで `REC` を押さない運用では測位が
始まらず、記録されたのは走行後に止まっていた端末の 2 塊だけ（79 点 / 90 分 / 移動 41m）。
**「停止・取込」を押す 1 つの操作に直した**（記録の停止・測位の書き出し・一覧の
取り直し・回収）。「取込」が無反応だったのも同じ修正で直る —— 接続時の `ls` しか
見ておらず、走行中に増えたファイルを知らないままキューが空だった。

解析への実害は小さい。**`[17]` は `0D` と同じ信号で校正も継承できるので、基板のログ
単体で校正済みの車速が出る。** GPS が要るのは地図だけになった。

**コーストダウンは足りなかった。** ギヤを繋いだ燃料カットが 3 本（3.0〜4.4 秒、
減速 1.01〜1.14 m/s²）取れたが、**クラッチを切った惰行が 1 本も無い**ので空気＋転がりの
基準が取れず分離できない。しかも 3 本とも 3800〜5400rpm の低い段で、減速度が速度に
よらずほぼ一定 —— **エンジンブレーキが支配していて $v^2$ が見えていない**。
**要るのは 60km/h 以上からクラッチを切ったまま 30km/h 台まで落とす数十秒の惰行**で、
勾配の小さい道を往復して勾配を相殺する。

#### 運用の作り込み（2026-09-19）

実走 3 本を通して、**「キーを回すだけ」を実際に成立させるための穴**が次々出た。
どれも実機で踏んで直したもの。

| 踏んだ穴 | 直し方 |
|---|---|
| 転送がループを占有し、記録も表示も止まる | **非ブロッキング化。** 予算を `poll` の有無で変える（停止中 200ms / 稼働中 40ms） |
| 転送が 6.8KB/s しか出ない | **`Serial.print` が律速だった**（171B を 115200bps で 14.8ms）。データ行を Serial へ出すのをやめて **15.2KB/s** |
| 転送中に BLE が切れると Serial へ吐き続ける | **切れたら転送を捨てる**（1MB で約 170 秒ぶん `poll` を食っていた） |
| 1 本が大きいと接続した走行で取り切れない | **256KB で分割**（`<base>_PP.bin`）。取り直しの損失に上限がつく。`dt` の連鎖はまたいでも切らない |
| 満杯で記録が静かに落ちる | **`write()` の戻り値を見て止める。** 記録開始時の確保 4MB ＋ 走行中の底上げ 512KB |
| ECU が居ないと要求だけのログが 155KB/時 育つ | **最初の応答が返った要求から保存**（`rec on` は武装まで） |
| `auto` だとファイル名に日時が入らない | **NVS の台帳**（`sess`）。起動後に初めて `time` が入った時点で `epoch − millis()/1000` を 1 回書く。削除と同期 |
| 「取込」が無反応 | 接続時の `ls` しか見ていなかった。**「停止・取込」に変えて、記録停止・測位の書き出し・一覧の取り直し・回収を 1 つに** |
| `auto` 任せだと測位が始まらない | 基板が「記録中」と返した時点で画面を合わせ、測位も始める |
| 2 台目のスマホで毎回過去ぶんを落としにかかる | **初回接続で問う**（`DLする` / `DLしない` →「消す？」）。どちらでも以後キューに入らない |
| 接続はできるが切断できない | 接続ボタンを状態ボタンに |

**設計判断として残しておきたいものが 2 つ。**

**ファイル名を後から改名しない。** 日時を名前に入れる案は、**背景転送と衝突する** ——
回収した後に改名すると受け手には新しいファイルに見えて同じ中身をもう一度落とす。
だから台帳で持つ。

**`auto` は起動時にしか走らない。** 「停止・取込」の後に記録が再開しないのは意図した挙動で、
転送完了後に自動再開する作りだと**回収後からキーオフまでの要らないデータが毎回溜まる**。

#### 表示の橋渡し（2026-09-19）

**`tools/klcsv.py`** が基板のログをビュワー用 CSV にする。`0x11` の 1 フレーム = 1 行、
最も近い `0xD1` の駆動状態を添える。**列名を `map.html` の別名表に合わせてあるので
無改造で読める。** パートは名前順に連結、絶対時刻は先頭パートのヘッダか `note` アンカー
から。測位 CSV を絶対時刻で突合すれば地図も出る（緯度経度と速度は線形補間）。

**`skew_ms = 0` を出すのが効く。** ビュワーは rpm と車速の取得時刻ずれを補間で直すが、
独自層は同一フレームなのでずれが無い。0 を渡せば補正が恒等になる。

**`map.html` は `drive` 列があればそれを使う。** 実走 1 本で、駆動中に段が付いた割合が
90.0% → 99.4%、駆動していないのに段を答えた行が 0 になった。

#### 基板の現状（2026-09-19 時点）

| | |
|---|---|
| ファーム | `explore`。OTA 済み。`auto on D1 11 200` **有効** |
| LittleFS | 7 ファイル / 550KB（空き 11.6MB）。**実走 3 本ぶんは `private/logs/` へ回収済み** |
| セッション台帳 | **空。** 今回の変更以降に記録したセッションから埋まる |
| ページ | `rec.html` `v20260919e` |
| ケース | 仮組みプレートで運用中。**本組みは M2 × 20 待ち** |

#### 次

**① まず確認したいのは「着火」。** 応答が返るまでファイルを開かない作りにしたが、
**着火する側は机の上では試せなかった**（折り返しの応答はエコーとして消費される）。
次の走行の頭で、スマホの `REC` が **`待機`（青）→ `REC`（赤）** に変わるかを見る。
変わらなければ着火していない ——その走行は記録されない。

**② 「停止・取込」が 1 回で通るか。** 記録停止 → 測位の書き出し → 一覧の取り直し →
回収、まで。**③ 測位が走行と重なるか**（3 本目は 0 秒だった）。

**これから踏む動作モードと、それで埋まるものは
[`docs/probe-plan.md`](docs/probe-plan.md) に集約した。** 以下は要点のみ。

**残っている解析の宿題**

| | 要るもの |
|---|---|
| **走行抵抗の実測（CdA / Crr）** | **60km/h 以上からクラッチを切ったまま 30km/h 台まで落とす数十秒の惰行**。勾配の小さい道を往復して勾配を相殺。3 本目には 1 本も無かった |
| 噴射の絶対単位 | 値 × 回転数の積分と**給油量**の突合。1 タンク分 |
| `0x20` / `0xD0` の正体 | **`poll D0 20 11 D1` で 4 本同時**。アンカーを載せても落ちない（[`docs/probe-plan.md`](docs/probe-plan.md) の F） |
| index 8 の bit 2 | 未着手。**index 9 は解決**（ラジエータファン） |
| 誤答率の交絡 | ログを増やす。**率ではなく機構で語る**（1 本目 6.7% / 2 本目 12.1% / 3 本目 4.2%） |


### 成果の書き出し —— 図・写真・記事の構成（2026-09-20）

**みんカラへ書くための素材を揃えた。** 解析そのものは動かしていない。

**`tools/figs.py` を足した。** 独自層の実測から図を作る。`gearing.py` と同じ流儀
（stdlib のみ・SVG を直接記述）で、`private/logs` の実走 3 本と `probe.bin` を読む。

| | |
|---|---|
| `figA-tables.svg` | テーブル総当たり 16×16。256 本中 7 本 |
| `figB-block.svg` | 基板のブロック図（`netlist.md` から起こした。ログに依らない） |
| `figC-injection.svg` | 噴射時間。空吹かしと走行の燃料カット |
| `figH-speed.svg` | 車速バイト `[17]` と GPS |
| `fig6-drive.svg` | 駆動状態で分けた比の散布。**⑧⑨の主役** |
| `fig7-band.svg` | 比が原理的に解けない帯の拡大 |
| `fig8-tolerance.svg` | 許容の内訳と誤答 |
| `fig9-gate.svg` | 駆動状態ゲートの前後 |

判定は `map.html` の ELM327 経路と同じ式で、**変速レート制限は掛けていない**
（`gear.md`「真値と突き合わせた」の集計に入っていないため）。同じ条件で `gear.md`
の数値を 1pt 以内で再現する。

**`_save()` に viewBox のはみ出し検査を入れた。** `figA` で 16×16 の格子を枠の外へ
描いていて、ビューアが黙って切るので気づきにくかった。以後は書き出し時に落ちる。

**公開する図の車速に上限を置いた。** 高速道路の最高法定速度を超える点は出さない。
`figs.py` / `gearing.py` の両方に `SPD_MAX_PUB = 120`。

**`fig5-usage.svg` の母集団を 2026-08-30 の 3 走行に固定した。** それまで
`mc52_*.csv` を全部拾っており、**図（8 本 27,083 点）とすぐ下の表（3 本 13,153 行）が
別のものを指していた**（凡例の「実走 3 本」は固定文字列で、数だけ全ログのものが
出ていた）。3 本に絞ると凡例が `実走 3 本 10,955 点` となり表と一致する。
この母集団の最高は 117.3km/h なので 120 の上限には掛からない。

#### FINDINGS の最小二乗をひとつ撤回した

`[17] = 0.9332 × GPS − 0.216` が**再現しない。** 同じ 2 本を `klcsv.py` の突合で
当て直すと、フィルタを振っても傾き 0.90 前後・切片 +0.7〜+1.3 にしかならない
（切片あり `0.8974 × GPS + 1.252`、原点通し 0.9306）。中央比のほうは 0.9346 で
記載の 0.9321 とほぼ一致する。

**切片は 20km/h 未満の GPS 雑音に引かれたもので、本書が `0D` について既に書いて
いた指摘（`0.9194 × GPS + 1.03`）と同じ形。** `FINDINGS.md` に撤回節を足し、
帯ごとの中央比の表と `figH-speed.svg` に置き換えた。**結論（`[17]` は `0D` と
同じ信号）は中央比だけで立つので変わらない。**

#### 写真

縮小（長辺 1600px）と **EXIF 全削除**を通してある。`photo-board-bench.jpg` には
自宅の GPS 座標が埋まっていた。**このリポジトリは PUBLIC なので、以後の写真も
必ず同じ処理を通す。**

| | |
|---|---|
| `docs/photo-connector-wide.jpg` | 引き。シート下の全景 |
| `docs/photo-connector-close.jpg` | 寄り。ELM327 → OBD2 メス → 白カプラ → 赤カプラ |
| `docs/photo-board-bench.jpg` | 机上試験。基板 ＋ UM232R |
| `docs/photo-case.jpg` | ケース入りの完成形。IC カードでスケール |
| `hardware/pcba-top.png` | JLCPCB の実装レンダ。`layout.png` より読める |

**未取得: 車載状態**（シート下に収めてタイラップで留めたところ）。⑦の整備手帳は
これが無いと出せない。`rec.html` の走行画面も未取得。

#### 解析レポートを 2 本に分けた（2026-09-21）

**[`docs/mc52-standard-layer.md`](docs/mc52-standard-layer.md)（前編）と
[`docs/mc52-proprietary-layer.md`](docs/mc52-proprietary-layer.md)（後編）。**
FL4 の `fl4-did-analysis.md` と同じ体裁（Abstract / Key Findings / 目次 / 本文 /
参考文献 / 更新履歴）。

**当初は 1 本にまとめたが、標準層と独自層を行き来して錯綜した。** 原因は真値評価が
両方に跨っていること（比推定＝標準層の産物を、駆動フラグ＝独自層の産物で採点する）。
**境界は「壁」に置いた。**

| | 前編 | 後編 |
|---|---|---|
| 主張 | 市販ドングルで何がどこまで取れるか、**そしてどこで止まるか** | 壁の向こうに何があるか、**そしてそれが前編の方法を採点する** |
| 再現コスト | ドングル ¥2,000 | 基板製作 |
| 状態 | **完結** | 宿題が残る（`nQ`・`[7]`・`5/6/7`・`0x20`/`0xD0`） |

**前編は独自層に一切触れない。** §4.4 は「解くには駆動が繋がっているかを示す信号が
要るが、標準層には無い」で止め、解を名指ししない。後編の存在は関連文書のリンク 1 行だけ。

**後編の導入は OSI で切った。** 規格と一致するのは第 1 層だけで、第 2 層以上は
フレーム構造・チェックサム・セッション・サービス番号のすべてが規格外。**この 1 行から
「同じ線で両方に届く」「市販品では 1 バイトも読めない」「両層は排他」の 3 つが導かれる。**

図の配分は後編に全部（`figA` `figB` `figC` `figH` `fig6`〜`fig9`）。`figD`（クランキング）と
`figE`（充電電圧）はバッテリーの話なのでどちらにも入れず、`FINDINGS.md` に置いたまま。
**前編は図なし** ——車速信号の同定と判定窓は図にできるので、後で足すか検討する。

数値は `tools/figs.py` が出すものに揃えた。**`FINDINGS.md` の誤答率（1 本目 6.7%）とは
0.1pt ずれる**（レポートは 74/1083 = 6.8%）。`gear.md` 側は誤答 72 件と数えており、
集計の経路が違う。結論に影響しないので追っていない。

`README.md` の冒頭に両方への入口を置いた。

#### 記事の構成

**2 部 9 本**（ブログ）＋ 整備手帳とパーツレビュー。文案は `drafts/minkara.md`
（ignore 済み・未公開）。

```
② ギアポジ → ③ 15T 検証 → ④ メーター誤差 → ⑤ バッテリー → ① 全数探索
  → ⑥ 届かない層 → ⑦ 基板 → ⑧ 独自層 → ⑨ 答え合わせ
```

①の結末（24 個で打ち止め）が⑥の動機になり、⑨が②へ戻って閉じる。整備手帳は
①（OBD に繋ぐ）と⑦（製作と取り付け）の 2 本。③はスプロケ交換の写真が無いので
ブログのみ。

続報として残したのは、噴射時間からの燃費、コーストダウンからの CdA / Crr 実測、
`0x20` / `0xD0` の 3 つ。

### Track B — 作り直し計画（2026-09-03 の記録。**実施せず**）

この計画は **2026-09-06 に破棄した**。作り直しは不要で、人手の介在も要らなかった
（上の「配線が通った」を見る）。判断誤りの記録として残す。

**自動配線が `NaN is not permitted` で落ちる。** 切り分けで規則・ネット表・層・パッド・
外形・禁止区域はすべて正常と確認済み（詳細は `embedded/knowledge/easyeda-api-pitfalls.md`）。
`dmt_Pcb.createPcb()` で API から作った PCB 文書に、GUI 経由なら入る初期設定が
欠けている疑いが残る。**PCB を正規の手順で作り直す。**

| | 作業 | 誰が |
|---|---|---|
| 1 | **GUI で新しい PCB を作る** | 人（数クリック） |
| 2 | **`Design` → `Import Changes` で回路図から取り込み、`Apply` を押す** | 人 |
| 3 | 配置・外形・禁止区域・圧入穴・取付穴をスクリプトで再現 | 自動（約 10 分） |
| 4 | **`Route` → `Auto Route`** | 人 |
| 5 | `U1` 周りの確認と手直し | 自動＋判断 |
| 6 | DRC | 人（`pcb_Drc.check()` が無いため GUI） |
| 7 | ガーバー / BOM / 実装座標の書き出し | 自動 |
| 8 | 発注 | **人。明示の GO があるまで触らない** |

**回路図は作り直さない。** 84 本すべてピン単位で検証済みで、一番手のかかる部分は
完成している。座標も全部記録してあるので、PCB 側は再現できる。

`U1` 周りは自動配線に任せない。`U1 → C1 → C3 → L1 → C4 → C5` の順に並べて入力ループを
最短にしてあるので、自動配線が遠回りさせたらその 6 点だけ手で引き直す。

**再現に使う記録**（`work/easyeda/`、リポジトリ外）

| ファイル | 中身 |
|---|---|
| `parts.json` | LCSC 番号 → EasyEDA のライブラリ UUID |
| `sch_state.json` / `pcb_state.json` | 参照名 → プリミティブ ID |
| `bbox_now.json` | 実測した本体外形 |
| `build_sch.py` | 回路図の配置とネット定義（`NETS` が設計の正本） |
| `build_pcb.py` `layout_pcb.py` `repack.py` | PCB の生成・配置・詰め直し |
| `export_mfg.py` `make_bom.py` `jlcstock.py` | 製造データ / BOM / 在庫照会 |

### やり残している設計上の宿題

- **`VCC = 3.3V` は試験値ではない。** データシートは 5V でのみ試験としている。最初の
  火入れで K ラインの読み書きが成立するか確認する。駄目なら `R1` を替えて 5V に上げるので、
  **`U3` の `RX` と ESP32 の間に分圧用の未実装パターンを PCB 設計時に入れておく**
- **取付穴を作り直す。** いま外してある。ランドと穴を同径（126/126）で作ったため
  環状部の幅がゼロで、NaN の候補として一度削除した。切り分けでは無関係と分かったが、
  作り方としては正しくない
- 降圧の熱設計が未計算
- **Extended 品種を減らすのは次版へ送る。** `C12`（1nF）は `C1523` で Basic 化できるが、
  9 品種のうち 1 つで 2 枚ぶん。手配料の差は基板代・実装費・送料を含めた総額に埋もれる。
  それより **16 行すべて照合済み・警告ゼロ・数量充足まで確認が取れた BOM を差し替えて
  再照合するほうが損**。1nF 50V X7R 0603 は電気的に同じもので、得るものが無い

### バッテリー交換で片付いたこと（2026-09-05）

TTZ8V（台湾ユアサ、2026-04-13 製造）へ交換。IG ON から接続したまま始動を記録した。

- **「クランキングは BLE リンクごと落とす」は撤回。劣化バッテリーの電圧降下由来だった。**
  IG ON 12.00V → クランキング 10.20V → 始動後 14.0V を**連続して記録**、`miss` も 0。
  健全なバッテリーなら「始動してから接続する」制約は不要
- **冷間かつ運転中の `0100` を取得。`0400` へ切り替わるのは水温 42℃ ちょうど。**
  これで冷間由来と負荷由来のオープンループが分離できた。過去のログで低水温域が
  `0400` 一色に見えていたのは、どれも 42℃ を過ぎてから始まっていたため
- 詳細は [FINDINGS.md](docs/FINDINGS.md)

**アイドルの安定性は、体感の改善をデータで確認できなかった。** 暖機後・停車・全閉での
回転数のばらつきは、旧 4 本が標準偏差 23.7〜61.8、新 4 本が 24.2〜50.9 で重なる。
そもそも**アイドル 1400rpm の単気筒は爆発が約 11.7Hz、こちらのサンプリングは約 1.4Hz**
なので、体感するザラつきは原理的に測れていない。肯定も否定もできない。

### バイクに行く際のついで

- サービス `21`/`1A`/`23`/`30` の総当たり（数分）
- 独自層の応答を 1 本キャプチャしてチェックサム規則を確定させる（**Track B の基板が
  要る。ELM327 では届かない層**）

---

## 保留事項

- **サービス `21`/`1A`/`23`/`30` が未試験。** 否定できたのはサービス22 だけ。優先度は低いが、
  スマホアプリで真のギヤ位置を出せる唯一の安い経路ではある。次にバイクへ行く際の数分で消化する
- ~~独自層の応答チェックサム規則が再現しない~~ → **解決**（2026-09-17）。実車の走査
  **256 フレームで 256/256 一致**。ダンプ側の誤記という [`firmware/PROVENANCE.md`](firmware/PROVENANCE.md)
  の判定（CRF の 4 件は先頭バイトの誤記、検算の通った応答はすべて先頭 `0x02`）も、
  実機の全応答が `0x02` だったことで裏付いた
- ~~冷間かつ運転中の `0100` が未取得~~ → **解決**（2026-09-05、42℃ が境界）
- `0909` が ECU 名 5 メッセージと返すのに `090A` は 4 行しか取れていない。BLE 通知の
  取りこぼしと判断してドレイン処理を追加済みだが、再取得はしていない
- ~~判定窓 ±4% で不確定が 3 割前後~~ → **解決。** 時刻ズレ修正・監視タイマ修正・量子化項の
  追加で確定 94%、さらに**判定窓を段ごと・上下ごとに独立させて 98.1%**（一律 4% は
  5-6 速境界で決まるので下の段では厳しすぎた。既存のラベルは 1 つも変えていない）。
  変速レート制限（`|Δgear| ≤ max(1, round(Δt/T))`）も入れた。
  **残りは半クラと変速過渡で、rpm と車速だけでは原理的に判定できない** ——
  減速中のクラッチ切りは 28km/h・1959rpm が真の 6 速と数値的に同一になる。
  **解は独自層の `0xD1` index 4 で、2026-09-19 の実走で決着した**（`00` 駆動 /
  `01` N かクラッチ / `03` スタンド）。比が原理的に解けない帯（20〜40km/h・
  1700〜2400rpm）で、駆動中は 6 速に集まり非伝達では 3/4/5/6 に散る。
  **ただし ELM327 経路には index 4 が来ない**（セッション排他）ので、
  `index.html` の判定は比推定のまま。index 4 は**真値として判定則の評価に使う**
- **走行余力そのものは未検証のまま。** 検証には車両が出しているトルクが要り、標準 OBD の
  範囲では取れない（`04`/`11`/`0B` のいずれも指標にならないことを確認済み）。**残る経路の
  独自層の噴射時間は、位置（`0x11` の [18-19]）まで取れた** —— アイドル 611〜648 で安定し、
  閉じた瞬間に 370 まで落ちる燃料カットの挙動も確認。**単位・endian・スケールが未確定**で、
  走行ログと突き合わせて決める
- ~~ギヤ位置（段数）が存在するかは未決~~ → **存在しない。** 実在する 7 テーブルに段数
  らしい値は無く、候補だった `0xD1` index 9 は**実走 2 本 3837 サンプルでもすべて `00`**
  （エンジン運転中・全段使用）。停車試験で付いていた留保は外れた。
  市販シフトインジケータが学習を要する比推定だったことと整合する

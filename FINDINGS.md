# CB250R（2018 / MC52）標準 OBD 層の実測結果

2026-08-23 実施。停車状態（IG ON のみ／アイドリング）での探索。生データは
`results/scan_20260823_1416.*`（エンジン停止）と `results/scan_20260823_1421.*`
（アイドリング、0x01-0xFF 総当たり）。

## 通信の同定

| 項目 | 実測値 |
|---|---|
| プロトコル | **ISO 14230-4 (KWP FAST)**、`ATDPN` = `A5` |
| 物理層 | K-Line 10400bps、シート下 4 ピン赤カプラ |
| ECU アドレス | `0x10`（応答ヘッダ `8N F1 10`、テスタ `0xF1`） |
| ドングル | ELM327 v2.1（BLE）、`ATRV` はバイクのバッテリ電圧を返す |

事前の推定（Euro4 なので ISO 14230-4 fast init）は実測で確認された。

ELM327 は ISO 14230 の**チェックサムバイトを落とさずに出力する**。形式バイト `0x8N` の
下位 6bit がモードバイト以降の長さなので、これで切らないと最終バイトをデータと誤認する。

## 対応 PID（サービス01）

サポートビットマップ:

| ベース | ビットマップ |
|---|---|
| `0100` | `FE3EF011` |
| `0120` | `80040001` |
| `0140` | `C8008001` |
| `0160` | `08000001` |
| `0180` / `01A0` / `01C0` | `00000001`（連鎖ビットのみ） |
| `01E0` | `00000000` |

**0x01-0xFF を総当たり（264 リクエスト）した結果、ビットマップ未申告で応答する PID は
存在しなかった。** 申告と実態が完全に一致する。取得可能なのは以下の 24 個で打ち止め。

アイドリング時（暖機中、ECT 42℃、1407rpm）の値:

| PID | 項目 | 生値 | 換算 |
|---|---|---|---|
| `01` | Monitor status since DTCs cleared | `00022000` | — |
| `02` | Freeze DTC | `0000` | — |
| `03` | Fuel system status | `0100` | オープンループ（暖機不足） |
| `04` | Calculated engine load | `69` | 41.2 % |
| `05` | Engine coolant temperature | `52` | 42 ℃ |
| `06` | Short term fuel trim b1 | `80` | 0.0 % |
| `07` | Long term fuel trim b1 | `80` | 0.0 % |
| `0B` | Intake manifold absolute pressure | `2A` | 42 kPa |
| `0C` | Engine speed | `15FC` | 1407 rpm |
| `0D` | Vehicle speed | `00` | 0 km/h |
| `0E` | Timing advance | `A6` | 19.0 deg |
| `0F` | Intake air temperature | `4F` | 39 ℃ |
| `11` | Throttle position | `1A` | 10.2 % |
| `12` | Commanded secondary air status | `01` | — |
| `13` | O2 sensors present (2 banks) | `01` | センサ1個 |
| `14` | O2 S1 voltage / STFT | `AC80` | 0.86 V |
| `1C` | OBD standards conformance | `28` | — |
| `21` | Distance with MIL on | `0000` | 0 km |
| `2E` | Commanded evaporative purge | `00` | 0.0 % |
| `41` | Monitor status this drive cycle | `00222020` | — |
| `42` | Control module voltage | `3520` | 13.6 V |
| `45` | Relative throttle position | `00` | 0.0 % |
| `51` | Fuel type | `01` | ガソリン |
| `65` | Auxiliary input/output supported | `0400` | — |

## 取れないと確認できたもの

「できない」の根拠を残す。

### エンジン停止では応答しないが稼働中は応答する

`0D`（車速）と `11`（スロットル絶対位置）は **IG ON のみでは 3 回リトライしても全て
`NO DATA`**。エンジン始動後は正常に応答した。ECU 側の欠落ではなく稼働条件の問題。
以降の探索は必ずアイドリング状態で行う。

### ビットマップに申告が無い（＝ECU が持たない）

`10` MAF、`1F` 稼働時間、`2F` 燃料残量、`33` 大気圧、`46` 外気温、`5C` 油温、
`5E` 燃料噴射量、`0A` 燃圧、`22`/`23` フューエルレール圧。

燃料残量が無いのは二輪では一般的。FL4 で行った `012F` のエッジ間推定は、この車両では
そもそも入力が無いため成立しない。

### サービス22（メーカー固有 DID）は全滅

`0000` / `F190` / `F1A0` / `0100` / `0101` / `F810` の 6 DID すべて `NO DATA`。
**Honda 独自データはサービス22 経由では取得できない。** FL4（CAN + サービス22）で
確立した手法はこの車両にはそのまま適用できない。

### 標準 PID に存在しない項目

**ギヤポジション、噴射時間（インジェクタ開弁時間）、バンク角、O2 ヒータ電流、
IACV/ETC ステップ** は標準 PID の定義に無く、当然取得できない。

みんカラの先行事例（下記「出典」）で「バンク角センサー（推定）」とされていた項目は、
少なくとも標準 PID の範囲では**否定される**。

## 車速信号の出どころ（ファイナルより上流）

**本車両は前スプロケットを 14T → 15T に変更してある。** そのとき、それまで GPS と乖離して
いたメーター表示が**ほぼ一致するようになった**という観測がある。ここから車速信号の出どころが
特定できる。

車輪速を見ているならスプロケを変えても車輪の回転そのものは変わらないので、GPS との関係は
動かないはず。**動いた以上、出力軸（カウンターシャフト）側の回転を見て、ECU が純正の二次
減速比とタイヤ外径を固定値として換算している**ことになる。

数字も閉じる。

| | |
|---|---|
| 前 14T / 後 36T | 二次減速比 **2.5714** ← 諸元表の 2.571 と一致。純正は 14T で確定 |
| 前 15T / 後 36T | 2.4000 |
| 丁数変更 | 15/14 = 1.0714。同じ車速で出力軸回転が 6.67% 低下 |
| ECU の換算 | 純正 2.571 前提のままなので表示は真値に対し **-6.67%** |
| 変更後に GPS と一致 | ⇒ **純正時のメーター誤差は +7.1%（= 15/14）だった**計算になる |

+7% は二輪のメーター誤差として一般的な値で、整合する。

### 帰結

**`k` はスプロケット丁数に依存しない。** `rpm / speed_obd` を構成する両方が**スプロケより
上流**にあるため。

- エンジン回転 → 出力軸回転は **一次減速比 × 変速比**のみで決まる。スプロケは関与しない
- 出力軸回転 → `speed_obd` は **ECU 内の固定係数**による換算。実スプロケも実タイヤも関与しない

**`k` はタイヤ外径にも依存しない。** 同じ理由で、摩耗も空気圧も銘柄も `speed_obd` に効かない。

したがって **`k` は物理量ではなく実質的にファームウェアの定数**で、車両間で安定するはず。
仕向地でキャリブレーションが違わない限り、**一度求めた `k` が他の MC52 でもそのまま使える**
可能性が高い。「タイヤの摩耗・空気圧・銘柄で k が変わるから各自で較正が要る」という当初の
前提は、この推論が正しければ崩れる（較正機能は検証手段として残す価値はある）。

### 前提としている構成

- **ECU が車速を検知するセンサは、OBD 出力とメーター表示で同一**
- **`0D` には生の計測値が出る一方、メーターには誤差要因を上乗せした値が表示される**

FL4 でも**メーター表示のほうが OBD 車速より速い**ことが確認されており、同じ構成と考えられる。
二輪・四輪を問わず、表示側だけに法規上の上乗せを入れるのは自然な作り。

### `0D` と GPS の比は、メーターとは逆向きに動く

上の構成なら、丁数変更が効くのは**生値のほう**で、メーターは表示補正で相殺される。

| | `0D`（生値） | メーター（上乗せ後） |
|---|---|---|
| 純正 14T | **≒ GPS（比 1.00）** | GPS より約 +7% |
| 本車両 15T | **GPS より約 -6.7%（比 0.933）** | ≒ GPS |

**本車両でメーターが GPS と一致したのは、表示補正（約 +7.1%）と丁数変更（15/14 = 1.0714）が
たまたま打ち消し合った偶然。** `0D` のほうはむしろ GPS から離れる向きに動いている。

したがって「メーターが GPS と合っているから OBD 車速も GPS と合う」とは**言えない**。
`0D` と GPS を突き合わせる場面では、この向きを取り違えないこと。

`tools/gearcal.py` の線形性検査は**水準ではなく速度域による変動**を見ているので、比が
1.00 でも 0.93 でも判定は変わらない。

### 実測で確認済み（2026-08-28、2 走行）

**予測は両方とも当たった。** 走行ログ 2 本（21.6 分 1308 行 / 10.2 分 631 行）での結果。

| | 予測 | 実測 | 差 |
|---|---|---|---|
| `k` | 1.000 | **1.0148 / 1.0116**（平均 1.0132） | +1.32% |
| `0D` / GPS | 0.9333 | **≒0.935** | +0.18% |

**`k` は 2 走行で 0.32% しか違わない。** 1 本のログ内だけの一致ではないので結論にしてよい。
6 速すべてにクラスタが立ち、予測位置からの偏差は最大 1.5%、ギヤ順の片流れも無い
（広がり 1.08% / 1.81%、R² 0.10 / 0.34）。単一 `k` で全 6 速の窓が決まることが実証された。

> **【2026-08-30 訂正】この `k` = 1.0132 は `0D` の切り捨てを吸収した値で、物理的な
> 解釈を載せてはいけない。** 下の「`0D` は切り捨て」を参照。真値を `0D + 0.5` として
> 取り直すと **`k` = 1.0057**、ECU 想定円周は **1.911 m**（幾何比 −0.57%）になる。
> 当初ここに書いていた「1.897 m、幾何より 1.3% 小さく荷重下の転がり円周として妥当」は、
> **大半が量子化の産物**だった。段ごとの `k` のばらつきも、生値では 0.83% あって
> 段番号に対して単調にずれていた（＝モデルが合っていない兆候）。

したがって:

- **`0D` は ECU 内部の固定係数による生の換算値**であり、メーター表示の上乗せは入っていない
- **メーター誤差は表示側だけの話**という前提（①②）が裏付けられた
- **`k` はファームウェアの定数**なので、丁数・タイヤに依存せず他の MC52 でも同じ値が使える

`0D`/GPS が予測とほぼ一致したことは、車速信号がファイナルより上流という結論の直接の裏付けにも
なっている。15T 化ぶんの 6.67% がそのまま出た。

### `0D` は四捨五入ではなく切り捨て（2026-08-30、13,153 行で同定）

**段ごとに `k` を独立に当てると、生値では段番号に対して単調にずれる。**

| | 2速 | 3速 | 4速 | 5速 | 6速 | 段間ばらつき |
|---|---:|---:|---:|---:|---:|---:|
| 生値のまま | 1.0213 | 1.0171 | 1.0154 | 1.0152 | 1.0129 | **0.83%** |
| `0D + 0.5` | 1.0056 | 1.0054 | 1.0057 | 1.0067 | 1.0057 | **0.13%** |

クラッチが繋がっていれば一次減速比 × 変速比は剛体連結で滑りが無いので、**`k` は全段で
同一でなければならない**。生値での 0.83% の単調なずれは、モデルが合っていないか測定に
偏りがあるかのどちらか。

低速ほど比が高く出るのは、**測定値が系統的に 0.5 km/h 低い**ことの症状。`0D + 0.5` を
真値として取り直すとばらつきが 0.13% まで潰れる。これは測定雑音の水準で、**変速比と
一次減速比だけで回転数と車速計測軸の関係が完全に説明できる**ことの確認になる。

独立した 2 走行で 0.82% → 0.10%、0.64% → 0.27% と同じように潰れるので、単発ではない。
定常点のみ（前後 2 サンプルで車速 ±1km/h・回転数 ±150rpm 以内）を使い、時刻ズレの影響を
排除している。

**帰結**

- **真値は `0D + 0.5` として扱う。** `k` = **1.0057**、ECU 想定円周 **1.911 m**
- **判定の許容に載せる量子化ぶんは ±0.5 のまま。** 切り捨て補正で消えるのは偏りであって
  不確かさではない（誤差は [0,1) から [−0.5,+0.5) へ移るだけ）
- `0D`/GPS の比も影響を受ける。生値 0.9408 に対し `0D+0.5` では **0.9489**。
  幾何予測 0.9333 との一致（+0.18%）を根拠にしていた記述は、**偶然の一致だった**。
  → **2026-09-05 に決着。下記「`0D`/GPS は丁数比より 0.9% 高い」を参照**

アプリ・ビュワーとも `v20260830f` で `0D + 0.5` と `k` = 1.0057 に入れ替えた。判定率は
新 3 走行で 94.9% → 95.1%、旧 2 走行で同等、曖昧点は 0.21% → 0.00%。

### `0D`/GPS は丁数比より 0.9% 高い（2026-09-05、9 走行で決着）

以前「旧 2 走行 0.9348 対 新 3 走行 0.9421」と 0.78% ずれて見えた件は、**ログ間の
ばらつきだった**。9 走行に増やすと旧期 5 本（0.9329〜0.9425）と新期 4 本（0.9381〜0.9489）が
完全に重なる。系統差ではない。

**速度帯ごとに見ると 45km/h 以上で平ら。**

| GPS 速度 | n | `0D`/GPS |
|---|---:|---:|
| 15-29 | 2,974 | 0.9667 |
| 30-44 | 6,410 | 0.9449 |
| 45-59 | 8,614 | **0.9401** |
| 60-74 | 4,395 | **0.9409** |
| 75-89 | 2,432 | **0.9410** |
| 90-104 | 2,848 | **0.9440** |
| 105-119 | 389 | **0.9431** |

低速側が高いのは `0D` の 1km/h 量子化と GPS 側のノイズ。**45km/h 以上では速度依存が
無く、定数のずれ**であることが確定した（「メーター誤差は比例、オフセット無し」の裏付け）。

**60km/h 以上で `0D`/GPS = 0.9421（n=10,065）。丁数比だけの予測 0.9333 より +0.94%。**

`0D` は車速センサ（ファイナルより上流）から ECU が固定円周で換算した値なので、

```
0D / 実車速 = (14/15) × (ECU 想定円周 / 実際の転がり円周)
0.9421 = 0.9333 × 1.0094
```

**ECU の想定円周が実際の転がり円周より 0.94% 大きい。** `k` = 1.0057 から求めた
ECU 想定円周 1.911m と組むと、**実際の転がり円周は約 1.893m**。幾何円周 1.9222m より
1.5% 小さく、荷重下のタイヤとして妥当。

**アプリへの反映:** 表示車速 `(0D+0.5) × 15/14` は GPS より **約 1.7% 高い**。丁数比の
ずれ +0.94% と `+0.5` ぶん +0.66% の合成。表示係数は**タイヤの摩耗・空気圧で動く実在の量**
なので、`k` と違い較正に意味がある。ビュワーに「車速校正」を置き、GPS から求めたトリムを
`localStorage` 経由で走行画面と共有する（既定は理論値。この車両の実測はトリム 0.9834）。

### メーター誤差は速度に比例する（オフセットが無い）

上の +7.1% を**換算係数として使えるか**は、誤差が比例なのか「比例＋定数」なのかで変わる。
国連規則 R39 は `表示 ≤ 真値 × 1.1 + 4 km/h` を許すので、定数項が入っている可能性はあった。

走行ログ 2 本の `0D` と GPS を速度帯別に見た（GPS 精度 15m 以内、両方 5km/h 超）。

| GPS 帯 [km/h] | n | `0D`/GPS 中央 |
|---:|---:|---:|
| 20–30 | 178 | 0.9405 |
| 30–40 | 201 | 0.9449 |
| 40–50 | 236 | 0.9368 |
| 50–60 | 169 | 0.9410 |
| 60–70 | 206 | 0.9401 |
| 70–80 | 137 | 0.9430 |
| 80–90 | 116 | 0.9427 |

**20 km/h 以上で比は平坦**（±0.4%、速度依存の傾向なし）。切片ゼロを強制した最小二乗で
**0.9371**。切片ありで当てると `0.9194 × GPS + 1.03` になるが、この切片は 20km/h 未満の
GPS 雑音に引かれたもので、帯別に見れば傾向が無いことから実体は無い。

パルス計数に固定係数を掛ける実装からしても比例が自然で、**定数項は無いと判断してよい**。
したがって上乗せは全速度域で一定の割合として扱える。

### メーター読みから実車速への換算

前提は 2 つだけ。**①車速信号はファイナルより上流**（本節冒頭で同定）、**②上乗せは比例**
（直前で実測）。ここに **③15T でメーターが GPS と一致する**（実測経験）を入れると、

```
メーター = B × ECU生値                     B はメーター内部の固定倍率
15T で メーター = 実車速                 →  B × 生値₁₅ = 実車速
センサはファイナル上流なので              →  生値₁₄ = 生値₁₅ × 15/14
∴ 14T では メーター = 実車速 × 15/14
```

| 装着 | メーター / 実車速 | 実車速への換算 |
|---|---:|---|
| 純正 14T | **15/14 = 1.0714**（+7.1%） | メーター読み **× 14/15** |
| 本車両 15T | **1.00** | そのまま |

**この 15/14 という値は `0D`/GPS の実測値を含まない。** 導出の途中で約分されて消えるため、
0.935 という測定の精度は換算に持ち込まれない。残る不確かさは③の「一致」がどれだけ厳密か
だけで、`0D`/GPS の幾何予測 0.9333 と実測 0.935 が 0.18% で合っていることから、
**0.9333 × (1/0.935) = 0.998** ——15T でのメーターと実車速のズレは 0.2% と見積もれる。

**丁数変更の副作用としてスピードメーターが正確になった**、というのが実態。狙って得た結果では
ない。アプリ側は `0D` に丁数補正（`15/14`）を掛けて表示しているので、**アプリの表示車速と
車両メーターは一致するはず**で、これは走行中にその場で確認できる。

### ギヤ判定の失敗は、加減速時の取得時刻ズレが主因

**エンブレ中（クラッチ接・スロットル閉）でも判定が落ちる**という体感があり、データで裏を取った。

| | 判定失敗率 | 失敗時の偏差（中央値） |
|---|---|---|
| ほぼ定常 | 27% / 15% | — |
| **加速中**（>+1.5km/h/s） | **75% / 75%** | **+7.3% / +8.0%**（正側 72%） |
| **減速中**（<-1.5km/h/s） | **70% / 65%** | **-5.8% / -5.4%**（負側 64〜73%） |
| うちエンブレ（スロ閉） | 73% / 77% | -5.8% / -5.3% |

**偏差の符号が加減速で反転する。** これは時刻ズレの決定的な signature で、クラッチとは無関係。
半クラなら比は一方向（回転が上がる側）にしか外れない。

原因はアプリ側の `pushRow` の位置だった。行を `0x0C`（回転数）の応答時に書いていたため、
その時点の車速は**1 周期前（約 520ms 前）の値**。設計時に「ズレは 1 要求ぶん ≒158ms」と
書いていたが、**実際は 4 倍あった**。

- 加速中 … 回転数が新しく高い／車速が古く低い → **比が過大**（＋）
- 減速中 … 回転数が新しく低い／車速が古く高い → **比が過小**（−）

既存ログで検証できる。行内の回転数を 1 行前の値に差し替えて時刻を揃えると:

| | 走行1 | 走行2 |
|---|---|---|
| 現状 | 失敗 43% | 失敗 34% |
| 回転数を 1 行前に | **29%** | **20%** |
| 2 行前（行き過ぎ） | 29% | 24% |

1 行ぶんでちょうど良く 2 行では改善しないので、ズレが約 1 周期であることも裏付けられる。

`v20260828d` で**行を車速の取り込み直後に書く**よう変更し、ズレを 1 要求ぶん（約 130ms）に
縮めた。残差は `skew_ms` 列として記録し、解析側で補正か棄却を選べるようにした。

**ギヤ判定に使うのは生値の比**である点は変えていない。表示車速のスプロケ補正は表示だけに
掛かる（補正済みを記録すると設定を間違えたときに戻せないため）。

### アイドル回転での偽の一致（回転数 1,700rpm 下限の根拠）

クラッチを切った惰性走行では回転数がアイドルに張り付いたまま車速だけが落ちるので、比が
**偶然どれかの段と一致する**。段が確定したように見えるが、そのときクラッチは繋がっていない。

新 3 走行で回転数の下限を外すと、確定 10,955 行のうち **72 行（0.66%）が 1,700rpm 未満**。
その内訳が偽陽性であることの裏付けは 2 つある。

- **72 行すべてがスロットル全閉**（この車両の `11` は全閉で 10.2%。72 行の中央値・最大とも
  10.2%）。駆動していない
- **散らばる段が下から順**（2 速 31 / 3 速 23 / 4 速 10 / 5 速 5 / 6 速 3）。車速の低下に
  つれて一致する段が下りていく形で、実際の走行としては説明がつかない

下限を 1,700rpm に置いたのは、**アイドル上限（約 1,500rpm）と 1 速の最小値の間**だから。
判定に使える最低速度（`0D` = 8、真値 8.5 km/h）での 1 速は 1,828rpm なので、正しい判定を
巻き添えにしない。
旧 2 走行では 3.00% が落ちる（判定率が低くアイドル張り付きの区間が相対的に多い）。

### 電圧が 15V を超えるのはバッテリー劣化の症状だった（2026-09-05 に訂正）

**当初の解釈は誤りだった。** 劣化した旧バッテリーでは `42`（ECU 電圧）の
**9.6% が 15V を超え、最大 15.9V** だった。これを「回転数と相関しないので、発電過剰では
なく単気筒のリップルを瞬時値でサンプリングした形」と解釈していたが、**バッテリーを
交換したら 15V 超が完全に消えた**。

| | 旧バッテリー（8/30、3 本） | 新バッテリー（9/5、5 本） |
|---|---:|---:|
| n（稼働中） | 13,147 | 19,406 |
| 中央値 | 13.80 V | 13.90 V |
| 標準偏差 | **0.592** | **0.278** |
| IQR | 0.60 | 0.30 |
| 最大 | 15.90 V | **14.80 V** |
| **15V 超** | **9.6%** | **0.0%** |

**ログ単位でも完全に分離する。** 旧 3 本が σ 0.575〜0.609 / 15V 超 9.3〜10.7%、
新 5 本が σ 0.260〜0.347 / 15V 超はすべて 0.0%。重なりが無く、走り方の違いでは
説明できない。

**機構:** 内部抵抗が上がったバッテリーは充電電流を吸収できず、レール電圧が上へ振れる。
「回転数と相関しない」という観察自体は正しかったが、そこから「だからリップル」と結論
したのが飛躍だった。**内部抵抗が高ければ、どの充電電流でも電圧は振れる。相関しない
ことは、むしろバッテリー側の原因と整合していた。**

**教訓:** 当時「正常と矛盾しない」で止めたのは妥当だったが、**バッテリー劣化の直接の
証拠は既にログに出ていた**。IG ON 11.6V で気づくより前に、電圧の分散が語っていた。
`volt` 列の分散は充電系ではなく**バッテリーの健全性**の指標として使える。

### アイドル回転数の分散は変わらなかった（測れていないだけ）

交換後に「アイドルが安定した」という体感があったので検証したが、**回転数の分散では
差が出ない**。整定後のアイドル（停車・全閉・75〜100℃・1250〜1550rpm・前サンプルとの
差 40rpm 以内）で比較した。

| | 旧（803 点） | 新（1,375 点） |
|---|---:|---:|
| 中央値 | 1401 rpm | 1402 rpm |
| σ | 20.2 | 19.6 |
| IQR | 28 | 25 |
| 隣接サンプル差の平均 | 25.3 | 25.4 |
| 同 90% 点 | 52 | 52 |

ログ単位でも旧 19.0〜21.6、新 17.2〜21.2 で完全に重なる。**電圧が完全に分離したのとは
対照的。**

**ただし体感を否定する根拠にはならない。** アイドル 1400rpm の単気筒は爆発が約 11.7Hz、
こちらのサンプリングは約 1.4Hz で、**燃焼ごとのばらつきは原理的に測れていない**。
むしろ機構としては体感を支持する材料がある —— 旧バッテリーはレール電圧が 9.6% の時間で
15V を超え σ が 2 倍以上振れており、点火コイルの充電電圧が安定しない。**アイドルは
発電量が最も少ない領域**なので影響が出やすい。

**回転数の分散として変化が無いことと、燃焼が安定したかどうかは別の問題で、この測定では
区別できない。** 判定するには筒内圧か、少なくとも爆発周期より速いサンプリングが要る。

### トルクの増加も検出できなかった（交絡に注意）

「トルクが太くなった」という体感も検証した。**エンジンが出力している領域では差が出ない。**

**アイドル負荷は指標にならない。これは測る前に分かること。** アイドルは空気量の
フィードバックを持たない（吸気は全閉の固定量で、ECU が回すのは燃料と点火時期）。
燃焼が改善しても `04` は動かない。

実際そうなっている。停車・全閉・80〜95℃ で `04` の中央値は旧 40.8 / 新 40.8、
σ 0.35 / 0.36 で、**`load` の σ は量子化 1 段（0.392%）ぶんしかないのに `rpm` の σ は 20**。
空気量が固定で回転数のほうが揺れる、という形がそのまま出ている。

**加速度で見る。** `0D` は 1km/h 刻み・1.3Hz で分解能が約 0.09 m/s² しかないため、
**`speed_gps` の微分**を使う（ギヤ・回転数 500rpm 帯・スロットル 5% 帯で揃え、窓内で
スロットルが 3% 以内に安定している点のみ、水温 80℃ 以上、GPS 精度 15m 以内）。

| | 枠 | 新>旧 | 差の中央 | 片側 p |
|---|---:|---:|---:|---:|
| 全枠 | 32 | 22 | +0.043 m/s² | **0.025** |
| **スロットル 15% 以上のみ** | 12 | **6** | **−0.002 m/s²** | **0.613** |

**全枠での「有意」は交絡だった。** 効果はほぼすべてスロットル 10%（＝全閉）の枠に
集中しており、そこで見ているのは**減速の度合い**であってエンジンの出力ではない。全閉で
どれだけ減速するかは勾配と乗り方で決まる。**エンジンが実際に出力している枠だけを見ると
完全にゼロ。**

**ここで止めずに分解したのが要点。** 22/32・p=0.025 だけを見ていれば、体感を「確認した」
と誤って報告していた。

**体感を否定する根拠にはならない。** 上と同じで、燃焼の質は 1.3Hz の OBD では測れない。
機構（点火コイルの充電電圧が旧バッテリーでは振れていた）も残っている。判定するには
**Track B の噴射時間**か、燃焼周期より速い観測が要る。

### `03` 燃料系状態の実測（2 走行で再現）

| 値 | 意味（J1979） | 出現状況 |
|---|---|---|
| `0100` | 暖機不足によるオープンループ | **停車時のみ観測（22〜42℃）。走行ログには出ない** |
| `0200` | **クローズドループ**（O2 フィードバック） | 走行中・定常 81% / 86%、加速 69% / 66% |
| `0400` | 負荷または減速燃料カットによるオープンループ | **停車 97% / 98%**、**エンブレ 90% / 70%**（強い減速では 91% / 89%） |

**`0400` には 2 つの原因が同居している。**

1. **アイドル。** 停車中はほぼ 100% が `0400`。単気筒は排気脈動が大きくアイドルで O2
   フィードバックが安定しないため、オープンループにする作りは一般的
2. **減速燃料カット。** スロットル閉で減速中も `0400` に落ちる。強い減速ほど比率が上がる

どちらも J1979 の `0x04`（負荷条件によるオープンループ）の定義どおりだが、**値だけでは
区別できない**。スロットル開度と車速の変化と併せて読む必要がある。

**`0100` が走行ログに出ないのは、暖気済みだったからではない。** 朝の走行（JST 08:46）は
コールドスタートで水温 45℃ から 92℃ まで上がっているが、それでも `0100` は 0 回。
**記録開始時点で既に 45℃あり、しきい値を過ぎていた**ため。停車時のスキャンでは 35〜42℃ で
`0100` を観測しているので、しきい値は 42〜45℃ のどこか。

原因は手順にある。接続と GPS 測位を待つ間に 1 分ほど暖機が進む。

**当初ここに「エンジン始動の直前に接続と測位を済ませておけばよい」と書いていたが、これは
誤り。** 始動のクランキングで BLE リンクごと落ちるため、その手順では取れない（後述の
「クランキングは BLE リンクごと落とす」を参照）。始動してから接続するしかなく、その時点で
既に暖機が進んでいる。**冷間かつ運転中の `0100` は未取得のまま。**

### IG ON・エンジン停止での `0100`（2026-08-30）

始動前の IG ON 区間を 94 秒記録できた（`results/log_20260830_0933_igon_cold.csv`、161 行）。

| 項目 | 値 |
|---|---|
| 回転数 | **終始 0 rpm**（エンジン停止） |
| 水温 | **22 ℃**（全区間一定） |
| `03` | **全 138 サンプルが `0100`** |

**エンジンが回っていなくても `0100` を返す。** 観測下限も 35℃ から 22℃ へ下がった。
ただしこれは「冷間で運転中のオープンループ」ではなく**始動前の初期状態**であり、
走行ログに `0100` が出ない件の直接の説明にはならない。

なおこの区間の ECU 電圧は 11.6 V。同じ IG ON でも 8/23 は 13.6 V だった。

**充電系は白。落ちているのはバッテリー側。** 同日の 3 走行 13,153 行で、エンジン稼働中に
12.5 V を割った点は**ゼロ**（最小 12.9 V）、アイドル中の中央値は 13.9〜14.0 V、全体の中央値は
3 本とも 13.80 V。発電・レギュレータが正常なら止まっている間だけ電圧が落ちるのは
バッテリーの充電受入れ側の劣化を示す。ただし `42` は瞬時値なので（後述の 16 V 超の件）、
断定するには端子電圧を直接測る必要がある。

またこの区間の 80〜94 秒あたりで ECU が応答を止めている。IG ON 放置でのタイムアウトか、
ドングル側のセッション切れかは切り分けていない。

水温帯ごとの内訳（朝のログ）: 0-50℃ と 50-60℃ は `0400` 100%、60-70℃ で `0200` が 40% 現れ、
80℃以上で `0200` 54% になる。

### 冷間由来と負荷由来のオープンループを分離した（2026-09-05）

**IG ON から接続したまま冷間始動を記録できたので決着した**（`mc52_2026-09-05_141428.csv`、
一晩置いた 31℃ から）。

```
t=  0.7s  ect=31℃  rpm=   0   0100            ← 冷間由来のオープンループ
t= 65.5s  ect=42℃  rpm=1445   0100 -> 0400    ← ここで切り替わる
t=498.8s  ect=80℃  rpm=2631   0400 -> 0200    ← クローズドループ開始
```

| 水温 | n | 内訳（運転中のみ） |
|---|---:|---|
| 30-39℃ | 55 | **`0100` 100%** |
| 40-49℃ | 75 | `0400` 72% / `0100` 28%（遷移中） |
| 50-59℃ | 75 | `0400` 100% |
| 60-69℃ | 97 | `0400` 100% |
| 70-79℃ | 239 | `0400` 93% / `0200` 6% |
| 80-89℃ | 250 | `0400` 81% / `0200` 18% |

- **`0100`（水温不足によるオープンループ）は運転中にも出る。終わるのは 42℃**
- **それ以前の観測で低水温域が `0400` 一色に見えていたのは、どのログも 42℃ を
  過ぎてから始まっていたため。** 冷間由来のオープンループを取り逃していただけで、
  存在しなかったわけではない
- 50〜79℃ の `0400` は水温ではなく**負荷由来**（減速燃料カット等）。これで
  「冷間由来と負荷由来を分離できていない」という保留は解消した

なお `0400` をクローズドループと誤読していた時期がある（正しくは `0x02` がクローズド
ループ、`0x04` は運転条件によるオープンループ）。燃料補正 `06`/`07` が動くのは `0200` の
ときだけで、これとも整合する。

**Track B への繋がり:** 減速燃料カット中は噴射が止まっているはず。独自層で噴射時間が
取れたとき、`0400` かつ減速中の区間で噴射時間がゼロ近傍になっていれば**両方のデータが
互いの裏付けになる**。燃費推定でもカット区間を消費に数えないため精度に直結する。

### 線形性検査で踏んだ落とし穴

`tools/gearcal.py` の `0D`/GPS 検査は当初この 2 走行で誤って非線形と判定した。原因は
**検査対象の選び方**で、3 つ重なっていた。

1. **`k` フィット用の定常点を渡していた。** あちらは回転変化率で選ぶが、それは rpm/車速比の
   歪みを避けるための条件で、`0D` と GPS の比を見るのには偏った部分集合になる
2. **比の分子側にも足切りをかけていた**（`spd > 10`）。帯を決めているのは GPS 側なので、
   `0D` が小さい点だけが落ちて比が上へ偏る
3. 各点の比を平均していた。分母のノイズで上振れするため、帯ごとの**和の比** Σspd/Σgps に変えた

修正後は変動 1.0% / 1.6% で、どちらも「一定とみなせる」。**検査が落ちたときは、被検査対象より
先に検査自体を疑うこと。**

## 取得周期と欠測（2026-08-30、停車＋アイドリングで 3 回測定）

`tools/p3sweep.py` で要求前の待ち時間を掃引した。1 周期 = 速い層 3 個（`0C`/`0D`/`11`）＋
遅い層 1 個（`04`/`42`/`05`/`03` を持ち回り）、`ATST` は `0x14`（80ms）固定。アプリと同じ順序。

| 待ち | 掃引1（AUTO, n=25） | 掃引2（明示, n=20） | 掃引3（明示, n=50） | 周期 |
|---:|---:|---:|---:|---:|
| 0 ms | 25.0% | 5.0% | 8.5% | 741 ms |
| 30 ms | 23.0% | 21.2% | 19.5% | 855 ms |
| 55 ms | 15.0% | 7.5% | 2.0% | 991 ms |
| 80 ms | 14.0% | **0.0%** | **21.5%** | 985 ms |
| 110 ms | 5.0% | 3.8% | — | 1101 ms |
| **平均** | **16.4%** | 7.5% | 12.9% | |

掃引1 はプロトコルが `AUTO` のまま走っていた（`ATSP0` 既定）。掃引2・3 は `ATSP5` を明示。

### P3min 説は棄却

**待ち時間と欠測率に安定した関係が無い。** 決定的なのは同一条件の再現性で、

| 待ち | 掃引2 | 掃引3 | 差 |
|---:|---:|---:|---:|
| 0 ms | 5.0% | 8.5% | 3.5 pt |
| 30 ms | 21.2% | 19.5% | 1.7 pt |
| 55 ms | 7.5% | 2.0% | 5.5 pt |
| **80 ms** | **0.0%** | **21.5%** | **21.5 pt** |

同じ設定を測り直すだけで 0.0% と 21.5% を行き来する。**掃引全体の変動幅より、単一設定の
再現ばらつきの方が大きい。** この条件では待ち時間は欠測を支配していない。

当初の仮説（ISO 14230 の P3min ≒55ms を割って ECU に無視される）は、成り立つなら待ちに対して
単調に効くはずだが、そうなっていない。**棄却する。**

唯一 3 回とも再現したのは **30ms が悪い**（23.0 / 21.2 / 19.5%）ことだが、その両側の 0ms と
55ms が良いので「短いと落ちる」では説明できない。理由は不明のまま残す。

### プロトコル未確定の影響は「疑わしい」止まり

`AUTO` の掃引1 は平均 16.4%、明示した掃引2・3 は平均 9.9%。**差はあるが、同一設定の
再現ばらつき（最大 21.5 pt）と同程度なので、これだけでは確定できない。** 掃引1 の 110ms は
5.0% で明示条件の良い値と変わらない。

**「AUTO だと欠測が桁で増える」と言えるだけの根拠は無い。** 平均で悪い傾向があるという以上の
ことは、この 3 本からは言えない。切り分けるには同一条件を複数回ずつ測る必要がある。

ただし `ATSP5` を明示するコストはゼロなので、アプリ側は自動探索へ落とす前に張り直しを
試す実装にしてある（`v20260830a`）。

### 周期は待ちぶんだけ素直に伸びる

741 → 855 → 991 → 985 → 1101 ms。待ちを入れた分だけ周期が伸び、**記録レートは下がる**
（1.35 → 0.91 Hz）。したがって仮に欠測を減らせても記録は増えない。**欠測率と記録レートは
トレードオフ**で、「脱落を潰せば実効サンプリングが上がる」という当初の見立ては成り立たない。

記録レート（周期レート × `0C` 成功率）で選ぶと**待ち 0ms が 3 回とも最良**。これを既定に
した（旧既定 55ms は `mc52_p3_v` で一度だけ破棄）。

### 実走 43% の正体はアプリ側の監視タイマだった

当初「停車 10% と実走 43% の乖離は走行固有の原因か `AUTO` 落ちのどちらか」と考えたが、
**どちらでもなかった。** 同日、アプリで停車のまま 40 秒記録したところ **58.9%** を記録し、
実走を上回った。**乖離はアプリとツールの差**であって、走行の有無ではない。

原因は `index.html` のポーリング監視タイマ。700ms ごとに応答待ちを点検し、待っている
要求があれば**取りこぼしとみなして捨て、次の要求を撃っていた**。捨てた後に届いた応答は
`awaiting` が次の PID を指しているため誤って解釈され、失敗が数周期にわたって連鎖する。

先取りを 1.5 秒まで待つよう変更（`v20260830d`）した前後の比較。いずれも停車・約 40 秒。

| | 変更前 `v20260830c` | 変更後 `v20260830d` |
|---|---:|---:|
| 行数 | 22 | 54 |
| 実効レート | 0.60 Hz | **1.29 Hz** |
| 欠測率 | 58.9% | **5.7%** |
| 行間隔が公称 1 倍 | 42% | **88%** |
| 間隔の裾 | 11 倍まで | 3 倍まで |
| `miss` 中央 / 最大 | 2 / 23 | **0 / 2** |

変更前は行間隔が **0.52s と 1.7〜5.6s の二山**で、停滞側の行に `miss` 6〜23 が集中していた。
連続的に遅いのではなく**バースト状に失敗**する形で、これは先取り→誤割り当ての連鎖と整合する。
変更後は単峰になり、**欠測 5.7% はツールの停車実測（5.0〜8.5%）と同水準**。アプリとツールの
差が消えた。

**したがって過去の走行ログの脱落 37〜43% は、K-Line でも P3min でも走行条件でもなく、
アプリの監視タイマが主因だったと考えられる。** 好調時は 130ms/要求で回っており、700ms の
猶予は一見十分に見えたが、Android 側で時折それを超えていた。

**留保:** 各条件 1 回・約 40 秒・停車のみの比較で、機序はコードと分布の変化からの推定。
応答の遅延そのものを計測したわけではない。走行での再現は次のログで確認する。

なお `tools/p3sweep.py` にはこの監視タイマが無いため、上の掃引結果は影響を受けていない。
P3min の棄却はそのまま有効。

## クランキングでリンクが落ちるのはバッテリー依存だった（2026-09-05 に訂正）

**以下の「クランキングは BLE リンクごと落とす」は、劣化したバッテリーの電圧降下に
よるものだった。** バッテリーを交換して再試験したところ、**IG ON から接続したまま
クランキングを通過し、記録も途切れなかった**（`mc52_2026-09-05_141428.csv`）。

```
t=4.27s  rpm=   0  volt=12.00  ← IG ON
t=5.00s  rpm=   2  volt=10.20  ← クランキング
t=5.72s  rpm=1966  volt=10.20  ← 初爆
t=7.89s  rpm=1701  volt=14.00  ← 発電開始
```

行は連続で `miss` も 0。3 秒以上の欠測はこの区間に存在しない。

| | 旧バッテリー | 新バッテリー（TTZ8V、2026-04-13 製造） |
|---|---|---|
| IG ON | 11.6 V | **12.00 V** |
| クランキング中 | 記録できず（リンクが落ちる） | **10.20 V** |
| 始動後 | 14 V 台 | 14.0 → 14.5 V |

**クランキング中の 10.20V は下限ではない。** `42` の分解能は 0.1V だが、こちらの
サンプリングが約 1.4Hz しかないため、**瞬間的な最低値は取り逃している**可能性が高い。

**手順への影響:** 「始動してから接続する」という制約は**健全なバッテリーなら不要**。
IG ON から接続したまま始動でき、そのぶん冷間のデータが連続で取れる。ただしバッテリーが
弱ると再び落ちるので、落ちた場合はアプリが自動で再接続する（最大 20 回、`ATZ` ＋
明示 `ATSP5` で張り直し）。

## クランキングは BLE リンクごと落とす（旧記述。上記のとおり条件付き）

**エンジン始動の瞬間に、K-Line セッションだけでなく BLE 接続そのものが切れる。**

2026-08-30、IG ON の状態で接続してからエンジンを掛けたところ、初期化中の `ATDP` で
`未接続` になった。別の試行では BLE は保ったが全 PID が `NO DATA` を返し続け、
`ATZ` ＋ `ATSP5` で張り直すまで復帰しなかった。始動時の電圧降下でドングルがリセット
されていると考えられる。

**帰結: 始動前に接続しておく手順は無意味。** 始動してから接続するしかない。

`0100` の捕獲が繰り返し失敗していた理由もこれで説明がつく（本ファイルの `03` の節に
「エンジン始動の直前に接続と測位を済ませておく」と書いていたが、その手順では取れない）。

なお ELM327 は IG OFF で BLE から消える。給電はイグニッション連動。

## 車両識別情報（サービス09）

| PID | 内容 | 値 |
|---|---|---|
| `0903` | CalID メッセージ数 | 4 |
| `0904` | Calibration ID | `0102A20601` |
| `0905` | CVN メッセージ数 | 1 |
| `0906` | CVN | `AA42A9BE` |
| `0909` | ECU 名メッセージ数 | 5 |
| `090A` | ECU 名 | `ECM -EngineContr…` |

`0902`（VIN）は**非対応**。したがって結果ファイルに車台番号は含まれない。

`0909` がメッセージ数 5 を返すのに `090A` は 4 行しか取れていない。BLE 通知の取りこぼしと
判断し、`send()` にプロンプト後のドレイン処理を追加した（再取得は未実施）。

## DTC

`03` / `07` ともに `00 00 00 00 00 00` で **故障コードなし**。`0A`（permanent）は `NO DATA`。

## 結論

標準 OBD 層は 24 PID で、エンジン制御の基本量は揃うが**二輪固有の情報（ギヤ、噴射時間、
バンク角）は一切含まれない**。走行中のロギング用途としては、回転数・車速・スロットル・
点火進角・負荷・O2 が取れるので下地としては使える。

# Honda 独自層（同じ K-Line 上の別プロトコル）

以下は**先行事例からの引用であり、CB250R では未検証**。実測で確定した上記とは区別する。

## ELM327 で届かない理由

「対応プロトコルの一覧に項目が足りない」のではない。物理層は同一（同じ K 線、10400bps、
同じ ECU）で、ELM327 のハードウェアに不足はない。

ELM327 は**プロトコルインタプリタ**であり、初期化ハンドシェイク・フレーム組み立て・
チェックサム・タイミングを自分で握る。K-Line 側に生バイトを流す透過モードが無い
（CAN 側の `ATMA` に相当するものが無い）。独自層は以下の 3 点すべてが ISO 14230 と
異なるため、ELM327 が組み立てるバイト列では ECU が独自層への呼びかけと認識しない。

| | Honda 独自層 | ELM327 |
|---|---|---|
| 起動 | K 線を **70ms Low** に落とす | ISO 14230 fast init は 25ms Low/25ms High 固定。任意長のブレークを出せない |
| フレーム | `72 05 71 XX CS` | 必ず `8N TA SA …` を前置する |
| チェックサム | **総和の 2 の補数** | ISO 14230 の単純和 |

## プロトコル仕様

'05 CBR600RR と CRF250L で**独立に解析され、内容が一致している**（下記 2 リポジトリ）。
車種をまたいで共通の作法とみられる。

初期化:

```
K 線を 70ms Low → 120ms High（アイドル）
→ ウェイクアップ  FE 04 72 8C     （CBR600RR では FE 04 FF FF）
→ 200ms 待つ
→ 初期化        72 05 00 F0 99
→ ECU 応答      02 04 00 FA
以降 2〜3 秒ごとに keep-alive が必要
```

テーブル読み出し: `72 05 71 <テーブル番号> <CS>`

チェックサム: 全バイトの総和の 2 の補数（`cksum -= data[i]`）。公開されている 5 フレーム
すべてで検算が一致する。

| フレーム | CS | 検算 |
|---|---|---|
| `FE 04 72` | `8C` | 一致 |
| `FE 04 FF` | `FF` | 一致 |
| `72 05 00 F0` | `99` | 一致 |
| `72 05 71 10` | `08` | 一致 |
| `72 05 71 D1` | `47` | 一致 |

## 内容が判明しているテーブル

- **`0x11`**（CRF250L の実ダンプあり）— RPM、TPS（電圧と %）、ECT（電圧と ℃）、IAT、
  MAP、バッテリ電圧、車速、**噴射時間**。センサ値が生電圧と物理量の両方で出る
- **`0xD1`** — 4 バイト目が噛み合い状態。`0x00`=イン / `0x01`=ニュートラルまたはクラッチ /
  `0x03`=サイドスタンド。**段数ではない**

`0x00`〜`0xFF` の全テーブル総当たりコードも公開されている。CB250R で最初にやるべきはこれ。

## 2 つの報告の突き合わせ（矛盾なし・同一プラットフォームと判断）

別人・別車種の報告が独立に一致する。決め手は交差検証で、**CBR600RR 側が定義したバイト位置を
CRF250L 側の実ダンプに当てると、物理的に妥当な値がすべて出る**。

| 位置 | CBR600RR の定義 | CRF250L の生値 | 換算 | CRF250L 側の注釈 |
|---|---|---|---|---|
| 4-5 | RPM | `00 00` | 0 rpm | 0rpm（車両 OFF） |
| 6-7 | TPS 電圧 / % | `1B 00` | 0.53V / 0% | TPS 0 |
| 8-9 | ECT 電圧 / ℃ | `9E 3D` | 3.10V / **21℃** | 21c |
| 10-11 | IAT 電圧 / ℃ | `9E 3D` | 3.10V / **21℃** | 21c |
| 12-13 | MAP 電圧 / kPa | `93 64` | 2.88V / **100kPa** | 100kpa |
| 16 | バッテリ電圧 | `69` | **10.5V** | 10.5v |
| 17 | 車速 | `00` | 0 km/h | — |

位置定義と実データが別々の報告者・別車種から来ているので、偶然の一致ではない。

**一致する要素**

- 初期化メッセージ `72 05 00 F0 99` がバイト単位で同一
- 起動手順（70ms Low → 120ms アイドル）が同一
- 読み出しコマンド `72 05 71 <テーブル> <CS>` が同一
- リクエストのチェックサム規則が 5 フレームすべてで一致
- 応答フレーム構造 `<b0> <長さ> 71 <テーブル> <データ…> <CS>`。**長さバイトが実バイト数と
  3 フレームすべてで一致**（25/25、15/15、10/10）

**異なる要素（車種差であって矛盾ではない）**

- **センサテーブルの番号**: CBR600RR は `0x10`、CRF250L は `0x11`。同じ内容が別番号にある。
  **CB250R でも総当たりで探す前提になる**
- **ウェイクアップのペイロード**: `FE 04 FF FF` / `FE 04 72 8C`。どちらも同じチェックサム
  規則で成立する正しいフレームで、中身の 1 バイトだけが違う

**未解決**

応答側のチェックサム規則が再現しない。リクエスト側は総和の 2 の補数で全フレーム合うのに、
応答は 3 フレームとも合わない（規則を 7 通り試して全滅、ズレ量も `0x11`/`0x07`/`0x71` と
不規則）。応答の 1 バイト目が `0x02`/`0x13`/`0x09` とばらつく点も未説明。公開ダンプが
バイト正確でない可能性がある。**実機で応答を 1 本キャプチャすれば即決まる。** 読み出しだけ
なら長さバイトで妥当性検証できるので、チェックサム検証は後回しでも動く。

## 独自層の利点は「同時性」であって速度ではない

**テーブル `0x11` は RPM と車速を同一フレームで返す。** ECU が同じ瞬間にサンプリングした値。

ELM327 経由では `0C` と `0D` が別リクエストになり最短 158ms 離れる。6 速・1000rpm/s の加速で
比が約 4% 歪み、ギヤ判定に必要な余裕 ±6% の大半を食う。同一フレームならこの誤差が消える。

つまり**定常走行でしか使えなかった比判定が、加速中・シフト中にも使えるようになる**。

レートは、実装例（下記）が `DEFAULT_SENSOR_INTERVAL 150`＝**約 6.7Hz** を採っている。
現状の 2Hz より速いが、効くのは同時性のほう。

`0xD1` の噛み合い状態は、比判定に欠けていた入力を埋める。標準 PID にクラッチスイッチが
無いため「比が無効な瞬間」を検出できなかったが、これが直接分かる。イン→ニュートラル→イン
の遷移はシフト事象そのものなので、ヒステリシスで数サンプル待たずに確定できる。

## 参照実装（CBR600RR 側、現役）

[andreibaw/Honda_K-Line_KWP2000](https://github.com/andreibaw/Honda_K-Line_KWP2000) は
**ESP32-S3 + PlatformIO の完成品**で、2026-08-20 に更新されている。Track B の直接の参考になる。

| | 内容 |
|---|---|
| ボード | ESP32-S3（`board = esp32s3_4mb`、PSRAM 有効、LittleFS） |
| 表示 | SSD1306 OLED（`GearFont.h` に大型ギヤフォント） |
| **スマホ連携** | **WiFi Station + ESPAsyncWebServer + WebSocket**。Web アプリは LittleFS 上（`data/index.html`, `app.js`, `style.css`）。**BLE は使っていない** |
| ギヤ判定 | `GearCalculator` が**比推定**。ギヤごとに min/max の帯を**学習**して NVS へ保存 |
| 状態ゲート | `GEAR_NEUTRAL(0)` / `GEAR_CLUTCH(-1)` / `GEAR_KICKSTAND(-2)` / `GEAR_UNKNOWN(-3)`。**ECU のフラグをゲートに使う** |
| 補助条件 | `SPEED_THRESHOLD 3` km/h。「クラッチレバーにスイッチが無いので、クラッチを握ったまま
ギヤが入っている状態を検出するために必要だった」とコメントにある |
| ポーリング | `DEFAULT_SENSOR_INTERVAL 150`（≒6.7Hz）、`DEFAULT_TABLE_INTERVAL 1000` |
| ライセンス | **GPLv3** |

`LiveDataPacket` が持つ値: rpm, speed, tps(%/V), ect(℃/V), iat(℃/V), batteryVoltage,
map(kPa/V), isEngineRunning, gearState。

**この実装が独立に同じ結論に達している点が重要。** 独自層から段数が直接取れるなら
`GearCalculator` は要らない。比推定＋ECU フラグのゲートという構成は、本リポジトリで
検討した設計と一致する。

なお本リポジトリの `tools/gearcal.py` は、ギヤごとに帯を学習するのではなく、**理論減速比を
軸に単一のスケール係数 k だけを当てはめる**方式を採っている。未知数が 1 個なので較正走行で
拾えなかったギヤも決まり、6 個のクラスタが予測位置に乗るかという反証手段も付く。

**GPLv3 なのでコードを流用する場合は本リポジトリも GPLv3 になる。** プロトコルの事実
（初期化手順・フレーム形式・バイト配置）を参照するだけなら制約は生じない。

# 未検証・未着手（記録として残す）

## 真のギヤ位置が存在するかは不明

以前このファイルに「市販シフトインジケータが同じコネクタからギヤ段を表示している以上、
独自層にギヤ情報がある」と書いたが、**これは誤りだったので取り消す**。

- **AIpro（MC52 専用）** — 取り付け後にニュートラルから 6 速まで各ポジションを設定する
  **学習操作が必要**
- **プロテック SPI-H45** — 配線が「スピードメーター信号」「エンジン回転信号」
  「ニュートラルランプスイッチ」の 3 本

どちらも**比からの推定**であり、ECU から段数を直接もらっていない。したがって独自層に段数が
あるかどうかは未確認のまま。判明しているテーブル `0xD1` も噛み合い状態だけで段数ではない。

決着は全テーブル走査でつく。2018 年式は点火マップ切り替えのためにギヤポジションセンサを
持つ可能性があるが（CRF250L の世代には期待薄）、確認していない。

## ELM327 で未試験のサービス

サービス22 の全滅から「独自データは UDS 経路に無い」と書いたが、否定できたのは**サービス22
だけ**。ISO 14230 セッションは確立済みなので、以下は ELM327 のまま叩ける。

| サービス | 内容 | 状態 |
|---|---|---|
| `22` ReadDataByIdentifier | UDS 流の 2 バイト DID | 試験済 → 全滅 |
| `21` ReadDataByLocalIdentifier | KWP2000 の古典。K-Line のメーカー固有データはここが定番 | **未試験** |
| `1A` ReadEcuIdentification | ECU 識別情報 | 未試験 |
| `23` ReadMemoryByAddress | アドレス直読み | 未試験 |
| `30` InputOutputControl | アクチュエータ制御 | 未試験 |

優先度は低い。当たっても各ローカル識別子の意味を一から起こすことになり、バイト単位で
解読済みの独自層に比べて実りが小さい。ただし**スマホアプリ（ELM327 経由）で真のギヤ位置を
出せる唯一の安い経路**ではあるため、次にバイクへ行く際の数分で消化する。

## 出典

- [sophienyaa/Honda-Motorcycle-ECU-Tools](https://github.com/sophienyaa/Honda-Motorcycle-ECU-Tools)
  — CRF250L。実ダンプと全テーブル総当たりコード（Arduino、未完成）
- [andreibaw/Honda_K-Line_KWP2000](https://github.com/andreibaw/Honda_K-Line_KWP2000)
  — '05 CBR600RR。初期化シーケンス、テーブル `0x10` / `0xD1`、および ESP32-S3 の完成実装。GPLv3
- [みんカラ「CB250R OBD2診断」（2022-04-09、初期型 ABS）](https://minkara.carview.co.jp/userid/203000/car/2716091/6833398/note.aspx)
  — **本リポジトリの出発点。** Panlong の BT アダプタ ＋ Car Scanner / Torque Lite で、
  回転数・電圧・吸気圧・吸気温・スロットル・水温・車速・O2 が取れること、シート下の赤
  カプラがサービスチェックカプラであること、DTC の読み取りと消去ができることを示している。
  **普段そのカプラにシフトインジケータが刺さっている**（一時的に外しても設定は消えない）
  という記述もあり、社外インジケータと OBD アダプタが同じポートを取り合うことが分かる。

  本リポジトリが足したのは、**`0x01`〜`0xFF` の総当たりによる「取れるものの全数」の確定**
  （24 個で打ち止め、未申告 PID はゼロ）、プロトコルの同定（`ISO 14230-4 (KWP FAST)`、
  ECU `0x10`）、サービス22 と VIN が非対応であることの確認、「バンク角センサー（推定）」の
  否定、そして車速信号の性質（ファイナル上流・切り捨て・メーター誤差 +7.1%）とギヤ判定。

# ネットリスト（MC52 K-Line ロガー）

EasyEDA の回路図から読み戻した実配置。ピン名は部品ライブラリのシンボル由来。

**ピン単位で照合済み** — ショート・ネット名の食い違い・未接続いずれも無し。

部品 28 点 / ネット 19 本

## VBAT — 2 端子

`J1.1`(1) , `D1.2`(A)

## VIN — 9 端子

`D1.1`(K) , `D2.1`(K) , `C1.1`(1) , `C3.1`(1) , `R8.1`(1) , `U1.3`(VIN) , `U3.7`(VS) , `C8.1`(1) , `R7.1`(1)

## SW — 3 端子

`U1.5`(SW) , `L1.1`(1) , `C6.2`(2)

## BST — 2 端子

`U1.6`(BST) , `C6.1`(1)

## EN_BUCK — 2 端子

`U1.2`(EN) , `R8.2`(2)

## FB — 3 端子

`U1.1`(FB) , `R1.2`(2) , `R2.1`(1)

## 3V3 — 14 端子

`L1.2`(2) , `C4.1`(1) , `C5.1`(1) , `R1.1`(1) , `U2.2`(3V3) , `U3.3`(VCC) , `C7.1`(1) , `C9.1`(1) , `C10.1`(1) , `R5.1`(1) , `R6.1`(1) , `R3.1`(1) , `R4.1`(1) , `J2.1`(1)

## KL_TX — 2 端子

`U2.10`(IO17) , `U3.4`(TX)

## KL_RX — 2 端子

`U2.11`(IO18) , `U3.1`(RX)

## KLINE — 4 端子

`U3.6`(K) , `R7.2`(2) , `C12.1`(1) , `J1.3`(3)

## DISP_CLK — 3 端子

`U2.17`(IO9) , `R4.2`(2) , `J2.3`(3)

## DISP_DAT — 3 端子

`U2.18`(IO10) , `R3.2`(2) , `J2.4`(4)

## DISP_CS — 2 端子

`U2.19`(IO11) , `J2.5`(5)

## DISP_DC — 2 端子

`U2.20`(IO12) , `J2.6`(6)

## EN_MCU — 4 端子

`U2.3`(EN) , `R5.2`(2) , `C11.1`(1) , `J3.4`(4)

## IO0 — 3 端子

`U2.27`(IO0) , `R6.2`(2) , `J3.5`(5)

## TXD0 — 2 端子

`U2.37`(TXD0) , `J3.2`(2)

## RXD0 — 2 端子

`U2.36`(RXD0) , `J3.3`(3)

## GND — 20 端子

`J1.2`(2) , `D2.2`(A) , `C1.2`(2) , `C3.2`(2) , `U1.4`(GND) , `U2.1`(GND) , `U2.40`(GND) , `U2.41`(GND) , `U3.5`(GND) , `C4.2`(2) , `C5.2`(2) , `C7.2`(2) , `C8.2`(2) , `C9.2`(2) , `C10.2`(2) , `C11.2`(2) , `C12.2`(2) , `R2.2`(2) , `J2.2`(2) , `J3.1`(1)

## 未接続

`U3.2`(LO) , `U3.8`(LI) , `J1.4`(4)

L9637D の `LI`/`LO` は L ラインで、この用途では使わない。データシートに
「`LI` 開放で `LO` は定義された状態になる」とあるので開放でよい。
